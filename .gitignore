# ==========================================
# 1. OS 및 IDE (루트 및 전체)
# ==========================================
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini
~$*

.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project

# ==========================================
# 2. 보안 파일
# ==========================================
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# ==========================================
# 3. Python 관련 (가상환경, 캐시, 빌드)
# ==========================================
**/venv/
**/.venv/
**/env/
**/ENV/

# 파이썬 캐시 및 빌드 파일
**/__pycache__/
**/*.py[cod]
**/*$py.class
**/*.so
**/.eggs/
**/*.egg-info/
**/dist/
**/build/
**/sdist/

# Jupyter Notebook 체크포인트
**/.ipynb_checkpoints/

# ==========================================
# 4. 프론트엔드 (Node.js)
# ==========================================
**/node_modules/
**/dist/
**/.vite/
**/.npm/

# ==========================================
# 5. Rust (크롤러 관련)
# ==========================================
**/target/
Cargo.lock

# ==========================================
# 6. ML/DL 및 데이터 (실험 로그 및 가중치)
# ==========================================
# MLflow 및 W&B, 로그
**/mlruns/
**/wandb/
**/*.log

# 딥러닝 가중치 (RoBERTa-base 기준 1개당 400MB+ → git 부적합)
**/*.pt
**/*.pth
**/*.ckpt
**/*.h5
**/*.onnx
**/*.safetensors
**/*.bin
ml/models/

# 캐시
ml/.cache/
.cache/huggingface/

# ml/ 원천 데이터: 기업마당 CSV 2종(23MB) + 중앙부처 엑셀.
# Temp/Downloads에서 재취득 가능하므로 제외한다.
ml/data/raw/

# 주의: 프로젝트 전체에는 **/*.parquet 나 **/data/ 같은 광범위한 규칙을
# 두지 않는다. ml/data/processed/*.parquet, ml/reports/*.json,
# ml/figures/*.png 는 전처리·모델 결과서의 근거 산출물이라 의도적으로
# 커밋 대상이다. 새로운 데이터 디렉터리를 만들 때는 이 절에 경로를
# 구체적으로 추가할 것.

# 추출 원문 JSONL(합계 106MB). 텍스트 자체는 announcement_detail_enriched.parquet
# 에 압축되어 들어가므로 커밋하지 않는다. e02_extract_text_v2.py 로 재생성 가능.
#
# 주의 — 커밋하지 않을 뿐 임시파일이 아니다. F04 아래 전 단계(F05·F06·M29)가
# 이 파일을 직접 읽는다. 지우면 하류가 재현되지 않는다. `_api` 를 빠뜨려
# 15MB 가 커밋될 뻔해서 와일드카드로 바꿨다.
ml/reports/e01_documents*.jsonl
ml/reports/e02_documents*.jsonl

# ==========================================
# 7. Docker 및 기타
# ==========================================
.docker/
docker-data/
