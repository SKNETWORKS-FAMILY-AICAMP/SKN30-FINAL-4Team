# 모델 4 — 사업 설계 이상패턴 탐지

> 과거 지원사업의 다변량 설계 패턴을 학습해 신규 사업의 희귀성·이례성을 탐지한다.
> **'이례적'이지 '잘못됐다'가 아니다.** 출력 문구를 코드에서 제한한다.

## 1. 학습 대상을 좁힌 이유

수치 4축(기업당지원액·지원건수·지원비율·사업기간) 중 2축 이상이 채워진 행만 쓴다.
결측을 메우고 전부 학습하면 모델이 잡는 것은 '희귀한 설계'가 아니라
'원문에 안 적힌 사업'이 된다.

| | 건수 |
|---|---:|
| 전체 (지원성격 확정) | 3905 |
| 학습·적용 대상 (2축 이상) | 2339 (59.9%) |

결측 축은 중앙값으로 채우되 `__missing` 지시자 축을 함께 넣었다. 지시자가 없으면
모델이 결측 행을 '중앙값 근처의 평범한 사업'으로 오해한다.

## 2. 모델 비교 — 합성 이상치를 잡는가

정답이 없는 비지도 모델이라 일부러 만든 이례 사례로 잰다.
(극소수기업·극고액 / 극다수기업·극소액 / 비정상 장기 / 지원비율 100%)

| 모델 | top-k 회수율 | top-2k 회수율 | 합성사례 중앙 순위 |
|---|---:|---:|---:|
| OneClassSVM | 78.3% | 45.8% | 상위 1.3% |
| LocalOutlierFactor | 63.3% | 39.2% | 상위 1.7% |
| IsolationForest | 8.3% | 7.5% | 상위 20.6% |

채택: **OneClassSVM**

### 설계서 권장(IsolationForest)을 쓰지 않은 이유

IF 점수가 무엇에 끌려가는지 축별로 쟀다(스피어만 상관, 절댓값 상위 5축).

| 모델 | 점수를 끌고 가는 축 |
|---|---|
| IsolationForest | `project_duration` 0.54, `project_duration__missing` 0.53, `log_per_recipient__missing` 0.51, `support_method__freq` 0.50, `amount_type__freq` 0.50 |
| LocalOutlierFactor | `log_support_count` 0.29, `category_large__freq` 0.20, `support_type__freq` 0.14, `support_ratio` 0.13, `amount_type__freq` 0.09 |
| OneClassSVM | `category_large__freq` 0.36, `project_duration` 0.32, `project_duration__missing` 0.32, `log_per_recipient__missing` 0.26, `log_support_count__missing` 0.26 |

IF 는 **결측 지시자와 희귀 범주 빈도**가 점수를 지배한다. 즉 '희귀한 설계'가
아니라 '원문에 안 적힌 사업'을 탐지하고 있었다 — N02 가 경고한 실패 모드 그대로다.
합성 이상치를 5건만 넣어도 회수율이 0% 여서 표본 수 문제도 아니었다.

## 3. 안정성

| 검증 | 결과 |
|---|---|
| contamination=0.01 상위 30건 유지율 | 100% |
| contamination=0.03 상위 30건 유지율 | 27% |
| contamination=0.05 상위 30건 유지율 | 30% |
| contamination=0.10 상위 30건 유지율 | 30% |
| (읽는 법) | IF·LOF 의 `score_samples` 는 contamination 과 무관하다. 그 모델들의 100%%는 안정성의 증거가 아니라 정의상 당연한 값이고, 이 검증이 의미를 갖는 것은 nu 가 모델을 바꾸는 OneClassSVM 뿐이다. |
| 80% 재표집 재학습(10회) 상위 30건 유지율 | 평균 83% / 최저 73% |

> 설계서의 '연도별 재학습 일관성'은 taxonomy 가 2023 단년이라 불가능해
> 표본 재추출로 대체했다. 대체했다는 사실을 여기 남긴다.

## 4. 상위 이상 사례와 축 단위 근거

| 점수 | 사업 | 상태 | 비교군 | 두드러진 축 |
|---:|---|---|---|---|
| 1.000 | [전남] 2025년 강소기업 육성사업 참여기업 모집 공고 | 과거 사업 패턴과 차이가 큼 | 사업화/grant (n=289) | 기업(과제)당 지원한도가 비교군 대비 높음 (P100, 성격x방식x단위x출처 비교군 289건)<br>지원비율이 비교군 대비 낮음 (P0, 성격x방식x출처 비교군 51건) |
| 1.000 | 2026년 방송광고비 할인지원사업 모집 공고 | 과거 사업 패턴과 차이가 큼 | 판로/other (n=75) | 사업기간이 비교군 대비 높음 (P100, 성격 비교군 75건) |
| 0.788 | 2026년 영세 소상공인 중소금융권 금융비용 지원사업 재공고 | 과거 사업 패턴과 차이가 큼 | 사업화/loan (n=377) | 지원비율이 비교군 대비 낮음 (P0, 성격 비교군 377건) |
| 0.689 | 2023년 중소기업 계약학과 국비지원 석사과정 모집 공고(경상 | 과거 사업 패턴과 차이가 큼 | 교육훈련/grant (n=957) | 지원비율이 비교군 대비 낮음 (P0, 전체 비교군 957건) |
| 0.659 | 2019년 1차 창업기업지원서비스 바우처 창업기업 모집 공고 | 과거 사업 패턴과 차이가 큼 | 사업화/voucher (n=None) | — |
| 0.645 | 2022년 중소의료기기기업 시장진입 단계별 규제극복 지원 | 과거 사업 패턴과 차이가 큼 | 성능인증/grant (n=957) | 지원비율이 비교군 대비 낮음 (P0, 전체 비교군 957건) |
| 0.643 | 2019년 3차 창업기업지원서비스 바우처 창업기업 모집 공고 | 과거 사업 패턴과 차이가 큼 | 사업화/voucher (n=None) | — |
| 0.635 | [전남] 2026년 소상공인 육성자금 지원계획 변경 공고 | 과거 사업 패턴과 차이가 큼 | 융자/grant (n=957) | 지원비율이 비교군 대비 낮음 (P0, 전체 비교군 957건)<br>사업기간이 비교군 대비 높음 (P90, 성격 비교군 236건) |
| 0.606 | 2019년 대ㆍ중소기업 혁신 파트너십 사업 공고 | 과거 사업 패턴과 차이가 큼 | 사업화/grant (n=51) | 지원비율이 비교군 대비 낮음 (P0, 성격x방식x출처 비교군 51건)<br>지원 기업/과제 수가 비교군 대비 낮음 (P10, 성격x방식x단위x출처 비교군 243건) |
| 0.584 | 일자리창출 우수기업에 대한 세정지원 공고 | 과거 사업 패턴과 차이가 큼 | 고용보조/grant (n=None) | — |

## 5. 출력 문구 제한

허용:

```text
과거 사업 패턴과 차이가 큼
희귀한 설계 조합
동일 유형 대비 비전형적
확인 필요
```

금지:

```text
잘못 설계됨
부적절함
정책적으로 문제 있음
지원규모 과다
```

`status` 값은 코드에서 이 목록으로만 생성된다. 자유서술을 붙이지 않는다.

## 6. 판정

**Conditional**

- 합성 이상치를 상위 k 안에 78% 회수
- nu(contamination) 를 0.01~0.10 으로 바꿨을 때 상위 30건 유지율 최저 27% — OneClassSVM 은 nu 가 모델 자체를 바꾼다. 상위 N 건을 고정 목록으로 쓰지 말고 nu 를 명시해 함께 출력해야 한다
- 80% 재표집 재학습 시 상위 30건 유지율 평균 83% (최저 73%)
- 전체 3905건 중 2339건(60%)만 학습·적용 대상 — 나머지는 수치 축이 1개 이하라 '이례'를 말할 근거가 없다
