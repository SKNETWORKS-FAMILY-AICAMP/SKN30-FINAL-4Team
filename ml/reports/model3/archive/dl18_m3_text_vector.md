# DL18 — 텍스트를 벡터에 붙인다

> 계획서 §4.1·§7. M38 은 정형 축만 썼습니다. 여기서 사업목적·지원대상·
> 지원내용을 embedding 으로 바꿔 같은 벡터에 붙이고, §5 의 A4(text-feature
> 불일치) 칸을 채웁니다.

```text
encoder  jhgan/ko-sroberta-multitask (CPU)
임베딩   768차원 -> PCA 24차원
블록가중 정형 0.6 / 텍스트 0.4
```

## 1. 텍스트 가중치를 데이터로 고르지 않은 이유

텍스트 가중치는 데이터로 고르지 않았다. M37 합성은 정형 축만 흔들어서 텍스트 가중치를 고를 수 없고, hold-out 으로 고르면 hold-out 이 튜닝셋이 된다. 계획서 §7 의 예시 비율 0.6/0.4 를 썼다.

즉 여기 실린 0.6/0.4 는 **계획서가 예시로 든 값**이지 실험이 고른 값이
아닙니다. 텍스트 가중치를 제대로 고르려면 텍스트도 흔드는 합성 생성기나
라벨이 붙은 두 번째 세트가 필요합니다.

## 2. 블록 조합별 — clean hold-out

| 블록 | 점수 | ROC-AUC | PR-AUC | 상위7 recall |
|---|---|---:|---:|---:|
| structured only | distance only | 0.9040 | 0.8135 | 0.7 |
| structured+text (0.6/0.4) | distance only | 0.8800 | 0.7730 | 0.5 |
| structured only | dist0.7+residual0.3 | 0.8720 | 0.7103 | 0.5 |
| structured+text (0.6/0.4) | dist0.7+residual0.3 | 0.8120 | 0.6407 | 0.5 |
| text only | distance only | 0.5320 | 0.5076 | 0.3 |
| text only | dist0.7+residual0.3 | 0.4640 | 0.3158 | 0.2 |

## 3. A4 — text-feature 불일치 (계획서 §5)

```text
text_mismatch = (자기 비교군 텍스트 중심까지의 코사인거리)
              - (가장 가까운 다른 비교군 중심까지의 거리)
```

양수이고 클수록 "텍스트는 다른 비교군에 속한다고 말하는데 정형 축은
이 비교군에 들어와 있다"는 뜻입니다. 비교군 12개를 텍스트 중심으로 잡았습니다.

| 점수 | ROC-AUC | PR-AUC | 상위7 recall |
|---|---:|---:|---:|
| 불일치 단독 | 0.5920 | 0.4932 | 0.2 |
| distance 0.7 + text_mismatch 0.3 | 0.8680 | 0.7103 | 0.5 |
| distance 0.5 + text_mismatch 0.5 | 0.8200 | 0.6764 | 0.5 |

> 이 신호의 값어치는 순위가 아닙니다. **경고 이유를 문장으로 말할 수 있다는**
> **것**입니다 — "사업목적 문장은 R&D 계열에 가까운데 설계 축은 융자형
> 비교군에 들어와 있습니다" 같은 문장이 여기서 나옵니다.

## 4. 코호트별 — 텍스트 정보량이 다릅니다

taxonomy 는 사업목적·지원대상·업종 원문이 있고 bizinfo 는 제목과 지원규모 문장뿐이다.

| 코호트 | 양성 | 음성 | structured only | text only | structured+text (0.6/0.4) |
|---|---:|---:|---:|---:|---:|
| taxonomy | 8 | 16 | 0.875 | 0.555 | 0.875 |
| bizinfo | 2 | 9 | 1.000 | 0.667 | 0.889 |

## 5. 판정

**텍스트를 붙여도 달라지지 않는다**

- 정형만 0.904 / 텍스트만 0.532 / 정형+텍스트 0.880
- 차이 -0.024 — 35건·양성 10건에서 이 정도는 부트스트랩 구간 안이다. 방향만 읽고 크기는 읽지 않는다.
- A4 text-feature 불일치 단독 ROC-AUC 0.592. 이 신호의 값어치는 순위가 아니라 **경고 이유를 문장으로 말할 수 있다는 것**이다.

## 6. GPU 가 필요한 지점

이 실험은 **CPU 로 끝납니다.** 사전학습 임베딩을 그대로 쓰기 때문입니다
(RoBERTa-base 로 1948문장 인코딩, 1~2분).
GPU 가 필요해지는 것은 **인코더를 미세조정할 때**입니다 — 텍스트로부터
설계 이상을 직접 학습시키는 단계로 넘어가면 그때 필요합니다.
