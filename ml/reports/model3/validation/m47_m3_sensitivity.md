# M47 — Model 3 민감도 확인 (대표벡터 · scaling · feature ablation)

> **새 모델을 찾는 실험이 아닙니다.** M44 에서 Freeze 한 구조가 임의의
> 구현 선택에 우연히 기대고 있는지, 거리 점수가 특정 feature 하나에
> 쏠려 있는지만 봅니다. (계획서 §5~§7)

## 1. 라벨 5건으로 어떻게 판정하는가 — 지표를 바꿨습니다

독립 라벨셋은 clean 53건 중 **양성 5건**뿐입니다. 그 위에서 변형별
ROC-AUC 를 비교하면 차이가 전부 표본 잡음에 묻힙니다(M44 CI 0.575~0.908).

그런데 이 실험이 실제로 묻는 것은 *어느 변형이 더 정확한가*가 아니라
**변형이 순위를 바꾸는가** 입니다. 순위 비교에는 라벨이 필요 없습니다.

| 지표 | 표본 | 라벨 필요 | 역할 |
|---|---:|---|---|
| Spearman 순위상관 | pool 1948행 | 불필요 | **주 근거** |
| Top-K 겹침 | pool 1948행 | 불필요 | **주 근거** |
| ROC-AUC / PR-AUC | clean 53건 | 필요 | 참고값 (우열 판정 불가) |

### 판정 문턱은 결과를 보기 전에 고정했습니다

기준은 M44 가 이미 측정한 **재표집 변동폭**입니다 — 80%% 재표집으로
대표벡터를 다시 만들면 상위 목록이 평균 91.8% 유지되고(= 8.2% 는 표본만
바꿔도 흔들림), ROC-AUC 는 0.738~0.779 사이에서 움직였습니다.

```text
Top30 겹침 >= 0.918      -> 표본 흔들림과 구별 안 됨 -> 기존 유지
ROC 차이  <= 0.0410       -> M44 재표집 폭 안        -> 기존 유지
```

## 2. 실험 4 — 대표벡터 `C` 를 무엇으로 만드는가

| 통계량 | Spearman | Top30 겹침 | Top39 겹침 | ROC(참고) | PR(참고) | 판정 |
|---|---:|---:|---:|---:|---:|---|
| `mean` | 1.0000 | 1.0000 | 1.0000 | 0.7542 | 0.2184 | 기준 |
| `median` | 0.9598 | 0.8333 | 0.8718 | 0.6625 | 0.1522 | 차이 큼 — 라벨 확대 후 재판정 필요 |
| `trimmed` | 0.9875 | 0.8667 | 0.8718 | 0.7208 | 0.2008 | 순위는 바뀌나 성능차는 변동폭 안 — 기존 유지 |

## 3. 실험 5 — feature scaling

> MAD 를 넣은 이유: `StandardScaler` 는 분산으로 나누는데, 이상탐지 대상인
> 극단값이 그 분산을 키워 **자기 자신을 덜 튀어 보이게** 만듭니다. MAD 에는
> 그 되먹임이 없습니다.

| scaler | Spearman | Top30 겹침 | Top39 겹침 | ROC(참고) | PR(참고) | 판정 |
|---|---:|---:|---:|---:|---:|---|
| `standard` | 1.0000 | 1.0000 | 1.0000 | 0.7542 | 0.2184 | 기준 |
| `robust` | 0.6732 | 0.3333 | 0.3077 | 0.7583 | 0.2393 | 순위는 바뀌나 성능차는 변동폭 안 — 기존 유지 |
| `mad` | 0.9682 | 0.7667 | 0.7692 | 0.7458 | 0.2295 | 순위는 바뀌나 성능차는 변동폭 안 — 기존 유지 |

## 4. 실험 6 — feature ablation

"모델이 특정 feature 하나에만 의존하는가?" (계획서 §7)

| 제거한 축 | Spearman | Top30 겹침 | Top39 겹침 | ROC(참고) | PR(참고) |
|---|---:|---:|---:|---:|---:|
| (제거 없음) | 1.0000 | 1.0000 | 1.0000 | 0.7542 | 0.2184 |
| `log_per_recipient` | 0.8597 | 0.7333 | 0.7436 | 0.7417 | 0.1915 |
| `log_support_count` | 0.7614 | 0.7333 | 0.7436 | 0.6542 | 0.1803 |
| `project_duration` | 0.9388 | 0.6000 | 0.5897 | 0.7792 | 0.2456 |
| `support_ratio` | 0.8877 | 0.6000 | 0.6923 | 0.7021 | 0.2189 |

### 축 의존도

`1 - Spearman` 이 클수록 그 축을 빼면 순위가 많이 바뀝니다.

| 축 | 의존도 |
|---|---:|
| `log_support_count` | 0.2386 |
| `log_per_recipient` | 0.1403 |
| `support_ratio` | 0.1123 |
| `project_duration` | 0.0612 |

## 5. ROC 차이와 경고 목록 변화는 따로 논다 (방향서 §9)

위 세 실험이 **부수적으로 보인 것**이 있습니다. 변형들은 경고 목록을
크게 바꾸는데, 라벨 기반 ROC-AUC 는 거의 움직이지 않습니다.

| 구획 | 변형 | 상위 30건이 바뀐 비율 | ROC 차이 |
|---|---|---:|---:|
| scaling | `robust` | **67%** | +0.0041 |
| ablation | `project_duration` | **40%** | +0.0250 |
| ablation | `support_ratio` | **40%** | -0.0521 |
| ablation | `log_per_recipient` | **27%** | -0.0125 |
| ablation | `log_support_count` | **27%** | -0.1000 |
| scaling | `mad` | **23%** | -0.0084 |

> `robust` 는 상위 30건 중 **67%(약 20건)가 다른 사업으로 바뀌는데도**
> ROC-AUC 차이는 **+0.0041** 입니다. 담당자가 받아 보는 검토 목록이 통째로
> 달라지는 변경인데 ROC 로는 보이지 않습니다.

**ROC 하나만 보고 모델을 바꾸면 안 되고, 실제 경고 목록의 안정성을**
**함께 봐야 한다** — 방향서 §9 가 이 표를 그 근거로 씁니다.

## 6. 같이 읽어야 하는 것

- ROC/PR 열은 **탐색적 보조 검증값입니다**(방향서 §7). 양성 5건이고
  도메인 전문가 Ground Truth 가 아니라 변형 간 우열을 가릴 수
  없습니다. 판정은 순위 지표로 했습니다.
- 이 실험은 성능을 올리려는 것이 아닙니다. 방향서 §8.3 대로 **"성능이
  조금 오른다고 바로 구조를 바꾸지 않고 경고 목록 stability 까지 함께
  본다"** 를 따랐습니다.
- 여기서 "기존 유지" 가 나오는 것은 **현재 구조를 바꿀 명확한 근거가
  없다**는 뜻입니다(방향서 §9). 우선순위는 구조 변경이 아니라 안정성
  검증(§13 1순위)으로 갑니다 — M48 에서 잽니다.
