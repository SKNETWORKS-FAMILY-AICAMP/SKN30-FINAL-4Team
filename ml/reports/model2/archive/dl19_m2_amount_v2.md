# DL19 — 모델 2(지원규모) 딥러닝 재대결 (M45 정제 타깃)

> DL14 의 판정은 M12 타깃 위에서 나온 것이라 지금 기준으로는 낡았다.
> 타깃을 M45 로 바꾸고 같은 규율로 다시 잰다.

## 1. 무엇이 달라졌는가

| | DL14 시점 (M12 타깃) | 지금 (M45 정제) |
|---|---:|---:|
| n | 2205 | 1877 |
| 비교군 중앙값 baseline | 0.6791 | 0.5368 |
| LightGBM | 0.5074 | 0.4645 |

타깃에서 고친 두 가지.

```text
1. per_recipient 에서 stated_cap(원문 한도)만 남기고
   budget_div_count(총예산/건수 = 평균)를 뺀다
   -> 비교군 안에서 이 둘은 9.97배 갈린다

2. 출처(cohort)를 비교군 사다리의 필수 축으로 올린다
   -> 섞으면 10칸 중 8칸이 유의하게 갈리고 최대 40배
```

`per_recipient_basis` 는 feature 에서 빠졌다 — 정제 후 전부 `stated_cap`
이라 상수축이다. DL14 는 이 축을 feature 로 갖고 있었다.

## 2. 같은 자로 쟀다

```text
GroupKFold(5) by program_stem — 셔플 없는 결정적 분할을 네 모델이 공유
타깃    log10(기업당 지원액) — M45 prepare(), stated_cap 만
feature 8개 범주 + 5개 수치 (M45 make_xy 그대로)
```

## 3. 결과

| 모델 | MAE(log10) | MedAE | 배수오차 | 2배 이내 | fold σ |
|---|---:|---:|---:|---:|---:|
| **LGBM-quantile50** (M45 채택) | **0.4645** | 0.3566 | 2.91x | 43.3% | 0.0109 |
| FT-Transformer (시드 3개 평균) | 0.5024 ± 0.0025 | 0.3743 | 3.18x | 41.7% | 0.0106 |
| Quantile MLP (시드 3개 평균) | 0.4883 ± 0.0038 | 0.3675 | 3.08x | 42.4% | 0.0106 |
| 비교군 중앙값 (baseline) | 0.5368 | 0.4260 | 3.44x | 34.6% | 0.0143 |

Quantile MLP 는 P10~P90 을 함께 학습하므로 구간도 나온다 — 커버리지 59.2%, 구간폭 중앙값 9.0배.

### 시드별

| 모델 | 시드 | MAE | 2배 이내 | fold σ |
|---|---:|---:|---:|---:|
| FT-Transformer | 42 | 0.4990 | 42.6% | 0.0083 |
| FT-Transformer | 7 | 0.5051 | 41.0% | 0.0108 |
| FT-Transformer | 2024 | 0.5030 | 41.7% | 0.0127 |
| Quantile MLP | 42 | 0.4913 | 42.5% | 0.0094 |
| Quantile MLP | 7 | 0.4906 | 41.6% | 0.0092 |
| Quantile MLP | 2024 | 0.4830 | 43.1% | 0.0131 |

## 4. 판정

판정선을 결과를 보기 전에 못박았다 — LightGBM 의 fold 간 표준편차(0.0109)보다
작은 차이는 분할이 흔들린 것과 구별되지 않으므로 개선이라 부르지 않는다.

| 도전자 | MAE | LGBM 대비 | 판정 |
|---|---:|---:|---|
| FT-Transformer | 0.5024 | +0.0379 | 미채택 — LGBM 보다 fold σ 이상 나쁘다 |
| Quantile MLP | 0.4883 | +0.0238 | 미채택 — LGBM 보다 fold σ 이상 나쁘다 |

**최종: LGBM-quantile50 유지**

## 5. 왜 이 조건에서 딥러닝이 불리한가

표본이 1877행이고 feature 가 13축이다. 사전학습 전이가 없는 신경망에게
2천 행은 적다. 모델 1에서는 KLUE-RoBERTa 가 한국어 사전지식을 이미 갖고
있어 900건으로도 ML 기준선을 넘었지만, 표 형식 수치·범주 데이터에는
그런 전이가 없다. 못 이겼으면 못 이겼다고 보고한다.

다만 이번 재대결로 확인된 것이 하나 더 있다 — **타깃 정제는 ML·DL 양쪽을
같이 끌어올린다.** 라벨 노이즈였던 '한도와 평균의 혼재'가 빠지면 두 계열이
모두 좋아진다. 모델을 바꾸는 것보다 라벨을 고치는 쪽이 컸다는 뜻이다.

실행 시간 3.6분 (CPU).
