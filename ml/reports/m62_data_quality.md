# M62 — 데이터 품질 감사·수정 (모델은 그대로 둡니다)

> 지시서 공통 원칙: **먼저 데이터 품질을 고친 뒤 같은 모델을 다시 평가한다.**

이 단계에서는 학습을 하지 않습니다. `design_features.parquet` 을 읽어 다섯 가지를
고치고 **`design_features_v2.parquet` 으로 따로** 내보냅니다. 원본을 덮어쓰지
않는 이유는 M56 canonical 의 데이터셋 지문이 살아 있어야 수정 전후를 같은 자로
비교할 수 있기 때문입니다.

| | sha256 (앞 16) | 행 |
|---|---|---:|
| 수정 전 `design_features.parquet` | `9f308112fb99e750` | 3963 |
| 수정 후 `design_features_v2.parquet` | `76407a5923e21de1` | 3963 |

## 0. 무엇이 잘못돼 있었는가

가장 큰 것 하나가 나머지를 전부 끌고 있었습니다.

**F06 `_pack_bizinfo` 가 목록 표본 1720행의 근거문 자리에 제목을 넣어 저장합니다.**
금액·지원비율·지원기업수는 F05 가 공고문 원문에서 뽑았는데, **그 원문이 하류로**
**넘어오지 않았습니다.** 그래서 F06 이 근거문에서 도출하는 두 파생값이 제목만
보고 만들어졌습니다 — 그 둘이 하필 **모델 2·3 비교군 사다리의 축**인
`support_method` 와 `support_unit` 입니다.

> 성능결과서 6장 2순위가 "파서가 아니라 저장 단계 문제"라고 적어 둔 바로 그
> 지점입니다. 파서를 고칠 일이 아니라 **원문을 다시 붙이면 되는 일**이었습니다.

## 1. 고친 것 다섯

| | 무엇 | 규모 |
|---|---|---|
| **D1** | 목록 표본 근거문을 제목 → 공고문 원문으로 복원 (F05 와 같은 문서 선택 규칙) | 1720행 복원 · 중앙값 2703자 (복원 불가 29행) |
| **D2** | 복원된 근거문에서 `support_unit` 재도출 | 결측 35.7% → **30.2%** · 신규 252 · 정정 107 |
| **D3** | 금액/비율 파싱 오류 제거 | 지원비율 범위밖 1 · 자부담률 1 · 사업기간 연도오파싱 93 |
| **D4** | 목록 표본의 `project_duration`·`self_burden_ratio` 복원 | 사업기간 +1400 (결측 85.4% → **50.1%**) · 자부담률 +181 |
| **D5** | 복원된 근거문에서 `support_method` 재도출 | 1720행 중 **848행 변경** |

### D5 가 가장 큽니다 — 비교군 축이 틀려 있었습니다

목록 표본은 근거문이 제목이라 `derive_method` 가 텍스트 증거를 하나도 못 보고,
**금액이 있다는 이유만으로 거의 전부 `grant` 로 떨어졌습니다.** 원문을 붙이자
이렇게 갈립니다.

| 이동 | 건수 |
|---|---:|
| `other` → `mixed` | 168 |
| `grant` → `loan` | 167 |
| `other` → `service` | 144 |
| `other` → `loan` | 113 |
| `other` → `grant` | 108 |
| `grant` → `guarantee` | 35 |
| `grant` → `voucher` | 28 |
| `other` → `guarantee` | 24 |
| `other` → `voucher` | 16 |
| `guarantee` → `loan` | 14 |

| 지원방식 | 수정 전 | 수정 후 |
|---|---:|---:|
| grant | 2118 | 1985 |
| guarantee | 89 | 136 |
| loan | 428 | 725 |
| mixed | 63 | 251 |
| other | 891 | 322 |
| service | 290 | 419 |
| voucher | 84 | 125 |

점수가 아니라 **모집단**이 바뀌는 오류였습니다. `grant` 에서 빠져나간 241행
(융자 167 · 보증 35 · 바우처 28 등)은 그동안 보조금 분포 안에서 percentile 을
계산받고 있었습니다.

> `derive_method` 의 두 인자를 F06 의 bizinfo 경로보다 한 단계 좁혀 넣습니다.
> F06 은 "나눌 근거가 없으니 같은 텍스트를 두 역할에 함께 넘긴다"고 적어
> 두었는데, 근거문이 복원되면 나눌 수 있습니다 — **현금 증거(`scale`)는
> 금액 문맥과 지원규모 절에서만** 찾고, 제공물 증거(`context`)만 문서 전체에서
> 찾습니다. 교육사업 설명문의 '사업비' 한 단어가 `grant` 를 만들어내는 자리를
> 막기 위해서입니다(F06 docstring 이 지목한 실패 모드).

### D2 — 지원단위는 '뽑힌 값'이라 근거를 남깁니다

근거 강도 순으로 3단입니다. 긴 문서 전체를 훑지 않습니다 — 2,700자짜리
공고문에는 '참여기업'이 어디든 한 번은 나오므로, 그러면 규칙이 아니라
**문서 길이가 단위를 정하게** 됩니다.

| 근거등급 | 무엇을 봤는가 | 행수 |
|---|---|---:|
| `amount_context` | 파서가 고른 금액 표현의 문맥 (금액과 같은 문장) | 804 |
| `scope_section` | '지원규모/지원내용/지원한도/…' 표제 뒤 160자 | 257 |
| `amount_type_convention` | `amount_type` → 단위 (현행 F06 fallback) | 50 |
| `none` | 근거 없음 — 결측으로 남깁니다 | 609 |
| `unchanged` | 근거문이 그대로라 손대지 않음 | 2243 |

우선순위도 하나 고쳤습니다. 현행 F06 은 `company` 규칙을 맨 앞에 두어
`개사` 가 `과제당` 을 이깁니다 — 실측: `지원규모 : 과제당 최대 90,000천원 /
2개사` 가 `company` 로 떨어집니다. 금액의 단위를 정하는 것은 '과제당'이므로
**'~당' 표현을 개수 표현보다 먼저** 봅니다. 어휘는 F06 것 그대로입니다.

> 표본 58건을 근거창과 함께 `m62_unit_audit.csv` 로 남깁니다. 근거등급별로
> 나눠 뽑았습니다 — 등급마다 정확도가 다를 수 있고 한 덩이로 뽑으면 그 차이가
> 평균에 묻힙니다. 대조해 보면 남는 오류의 유형은 하나입니다: **자격요건·성과
> 목표 문장의 숫자**(`상시근로자수 50명 미만인 사업장`, `신규고용 1억당 1.2명`)
> 를 단위로 읽는 것. 규칙으로 채운 값은 '맞는 값'이 아니라 '뽑힌 값'입니다.

### D3 — M61 이 지목한 1건이 여기서 처리됩니다

`지원비율` 최소값 **-320.0% → 0.0%**. 비율은 음수일 수 없으므로 파싱 오류이고,
M61 이 scaling 진단에서 찾아 "M31 파서 감사 대상"으로 넘긴 그 1건입니다.

사업기간은 **상식범위(0,10] 밖이면서 근거등급이 `bare` 인 것만** 지웠습니다
(93건). 근거등급이 `context`/`hint_only` 인 38건은 10년 융자처럼 실제로 긴
사업이 있어 남깁니다. `bare` 쪽의 정체는 M31/M32 가 고친 '연도 두 자리를
기간으로 읽는' 오류의 잔재입니다 — 2026년 공고에서 기간 26년.

- 2023년 해외실증(PoC) 지원 프로그램
- 2022년 뿌리기업 자동화·첨단화 지원사업
- [제주] 제주시 2026년 우리동네 맑은공기 패키지 지원사업 소규모 사업장 방지시설 지원사업 공고
- [대전] 2026년 3차 해외규격인증 획득지원사업 참여기업 모집 공고

## 2. 두 모델의 입력이 얼마나 달라졌는가

### 모델 2 — 대상 행은 하나도 안 변합니다

```text
수정 전  {'전체': 3963, '지원성격_결측_제외': 58, '기업당지원액_결측_제외': 1694, '파싱오류범위_제외': 84, '총예산나눗셈(평균)_제외': 250, '필수축_결측_제외': 0, '최종': 1877}
수정 후  {'전체': 3963, '지원성격_결측_제외': 58, '기업당지원액_결측_제외': 1694, '파싱오류범위_제외': 84, '총예산나눗셈(평균)_제외': 250, '필수축_결측_제외': 0, '최종': 1877}
```

타깃(`stated_cap`)·금액·필터를 건드리지 않았으므로 **1,877건 그대로**입니다.
그래서 M63 의 수정 전후 비교는 같은 행·같은 타깃·같은 fold 에서
**입력 품질만** 다른 순수 대조가 됩니다.

> `필수축_결측_제외 = 0` 이라는 줄도 기록해 둡니다. 지시서는 모델 2 의 1순위로
> `지원단위` 결측 15% 보완을 지목했는데, 모델 2 데이터셋에서는 그 결측이
> **행을 한 건도 떨어뜨리지 않습니다** — `stated_cap` 이면 `amount_type` 이
> per_company/per_project 라 단위가 관행으로 항상 채워지기 때문입니다.
> 15%는 **모델 3 pool 의 숫자**입니다. 모델 2 에서 `지원단위`가 하는 일은
> 행을 거르는 것이 아니라 **값이 맞느냐**이고, 그건 D2 가 고칩니다.

### 모델 3 — pool 이 늘어납니다

| | 수정 전 | 수정 후 |
|---|---:|---:|
| pool 행수 | 1948 | **2377** |
| `지원단위` 결측률 | 14.9% | 15.8% |
| 수치축 평균 개수 | 2.429 | **2.586** |
| log_per_recipient 결측률 | 8.4% | 21.5% |
| log_support_count 결측률 | 16.5% | 18.9% |
| support_ratio 결측률 | 54.5% | 60.9% |
| project_duration 결측률 | 77.7% | 40.2% |

D4 가 목록 표본의 사업기간을 복원하면서 수치축 2개 조건을 넘긴 행이 생겨
pool 이 1948 → 2377 로 늘었습니다. **결측률만 보면 `지원단위`가 나빠진 것처럼**
**보이는데, 분모가 달라진 것**입니다. 같은 행에서 재면 M64 가 보여줍니다.

## 3. 남겨 둔 것 — Open API 행의 근거문 불일치

같은 결함이 한 단계 약하게 남아 있습니다. Open API 738행 중 **407행**은
F05 가 금액을 공고문 원문에서 뽑았는데, F06 이 근거문 자리에는 CSV 요약
(`support_amount_raw` + `summary_text`)을 넣습니다. 이 행들도 *금액을 뽑은
텍스트와 근거문이 다릅니다.*

이번에 손대지 않은 이유는 **심각도가 다르기 때문**입니다. 목록 표본의
근거문은 제목이라 파생값이 텍스트 증거를 하나도 못 봤지만, API 요약은
실제 사업 설명이라 `support_method`·`support_unit` 이 근거 없이 만들어지지는
않습니다. 여기까지 같이 바꾸면 이번 재평가에서 *무엇이 무엇을 움직였는지*
가려낼 수 없게 됩니다. 근본 수정(F06 `_pack_bizinfo` 자체)을 할 때 함께
처리할 항목으로 적어 둡니다.

## 4. 바꾸지 않은 것

```text
타깃 정의(stated_cap)
비교군 사다리
모델·하이퍼파라미터
평가 프로토콜
design_features.parquet 원본
```

데이터를 고쳤다고 말하려면 모델·프로토콜은 그대로여야 합니다. 재평가는
M63(모델 2) · M64(모델 3) 가 합니다.
