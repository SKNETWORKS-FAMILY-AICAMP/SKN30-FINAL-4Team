# 중소기업 지원사업 Pre-Review — 모델 1·2·3 성능 결과서

**기업마당 공고를 지원성격별로 분류하고(모델 1), 신규 사업의 지원규모를 과거
사업과 견주며(모델 2), 사업 설계의 이례성을 짚습니다(모델 3).**

> 전체 실험 과정(후보 비교, 튜닝 시도, 뒤집힌 판정들)의 상세 기록은
> `ml/reports/`의 개별 리포트(m01~m49, dl01~dl19)에 그대로 남아 있습니다.
> 이 문서는 **현재 채택 상태와 그 핵심 근거만** 담습니다. 데이터 수집·정제는
> `01_데이터_전처리_결과서.md` 참조.
>
> **모델 3은 기획서 Product Boundary 정합성 검토로 정체성이 바뀌었습니다.**
> "설계가 정상/이상인지 최종 판정하는 모델"이 아니라 **"유사사업 비교군
> 대비 설계 조합의 통계적 이례성을 산출해 검토 우선순위를 보조하는
> 모델"**로 재정의됐습니다(3장). 그에 맞춰 검증 대상도 정답 대조 정확도가
> 아니라 **이례성 신호 자체의 안정성**으로 바뀌었습니다.

---

## 0. 요약

| 모델 | 과제 | 채택 | 핵심 성능 |
|---|---|---|---|
| **1** | 지원성격 분류 (19클래스) | **KLUE-BERT** (+ 판단보류) | 외부 131건 정확도 **0.8422 ± 0.0072** (커버리지 70%에서 0.9275) |
| **2** | 유사 비교군 내 지원규모 상대비교 | **비교군 percentile + LGBM-quantile50** | MAE(log10) **0.4681**(n=1,877), baseline 대비 **−11.9%** |
| **3** | 사업 설계 이상패턴 탐지 (이례성 신호 · 검토 우선순위 보조) | **구조 Freeze + 안정성 검증 완료, 운영 배포는 보류** | 재표집 순위상관 0.94~0.98(M48), stress test 단조성 100%(M49) — 정답 대조 ROC-AUC 0.7542(M44)는 탐색적 참고치, 경고 임계선 미확정 |

**공통 패턴**: 텍스트(모델 1)에서는 DL이, 표 형식 데이터(모델 2·3)에서는 ML이
통계/ML 기준선을 이겼습니다 — 사전학습 한국어 지식의 전이 여부가 갈림길입니다.
그리고 **교차검증·합성 벤치마크와 실제 검증(외부 정답셋, 사람 라벨)이 두
모델(1, 3) 모두에서 어긋났습니다** — CV/합성 점수만으로 배포를 결정하지 않은
것이 핵심 교훈입니다. 모델 3은 여기서 한 걸음 더 나아가, **"정답이 무엇인가"**
**자체가 불확실한 과제에서는 정답 대조보다 신호의 안정성을 검증하는 쪽으로**
방향을 옮겼습니다(3.3절).

---

## 1. 모델 1 — 지원성격 분류

**채택: KLUE-BERT** (그룹CV macroF1 0.8350 / 외부 131건 정확도 0.8422 ± 0.0072)

| 후보 | 계열 | 외부 131건 정확도 |
|---|---|---:|
| **KLUE-BERT** | DL | **0.8422 ± 0.0072** |
| KLUE-RoBERTa | DL | 0.8143 ± 0.0157 |
| TF-IDF + LinearSVM (ML 기준선) | ML | 0.7252 |
| KoELECTRA | DL | 0.7583 ± 0.0072 |
| SBERT + LogReg/SVM | DL(임베딩) | 0.7176 / 0.5878 |

BERT vs ML 기준선 McNemar p=0.024로 유의한 우위. `predict_proba`를 활용한
판단보류(임계값 0.20/0.35)로 커버리지 70%에서 정확도 0.9275까지 오릅니다.

검증은 `program_stem` 기준 StratifiedGroupKFold(재공고 그룹 누수 차단) +
Open API 외부 131건 holdout. **한 차례 판정이 뒤집혔던 이력**: 초기 41건
대조에서 RoBERTa가 51.2%로 무너져 ML(LogisticRegression)을 채택했으나,
원인은 모델이 아니라 **DL만 학습·서빙 텍스트가 어긋난 것**이었습니다. 입력을
맞추고 정답셋을 131건으로 늘리자 DL이 다시 앞섰습니다.

**폐기**: 지원규모 월별 시계열 예측 — 4종 전부 Last Value baseline에 크게
뒤짐(계절성 0.00) + 부처 실효성 부족. 대신 **관측 기반 참고범위**(중앙값·
P10~P90)를 제공합니다. 상세: `ml/reports/dl15_final_selection.md`.

---

## 2. 모델 2 — 지원규모 상대비교

**채택: 비교군 percentile + LGBM-quantile50** (MAE(log10) 0.4681, n=1,877)

| 모델 | MAE(log10) | baseline 대비 |
|---|---:|---:|
| **LGBM-quantile50** | **0.4681** | **−11.9%** (채택 기준 −10%) |
| Quantile MLP (DL) | 0.4883 ± 0.0038 | 미채택 — fold σ(0.0109) 이상 나쁨 |
| FT-Transformer (DL) | 0.5024 ± 0.0025 | 미채택 |
| 비교군중앙값 (baseline) | 0.5315 | — |

타깃은 `stated_cap`(원문 명시 한도)만 쓰고, 비교군 사다리에 **출처
(taxonomy/bizinfo, 칸에 따라 최대 40배 차이)를 필수 축으로** 둡니다. CQR
보정 후 예측구간 커버리지 79.6%, 구간폭 33.2배 — 비교군별 편차가 커서
(연구개발 8배 ~ 컨설팅/guarantee 331배) 구간은 기본 출력이 아니라 보조
정보로만 제공합니다.

**참고**: 초기 타깃은 `stated_cap`과 `budget_div_count`(총예산÷건수)가
섞여 있어 비교군 안에서 최대 9.97배 갈렸습니다. 이를 정제(M45)한 뒤 수치가
전반적으로 개선됐습니다 — 모델을 바꾸는 것보다 타깃(라벨)을 고치는 쪽이
효과가 컸습니다. 상세: `ml/reports/m45_m2_amount.md`, `dl19_m2_amount_v2.md`.

---

## 3. 모델 3 — 사업 설계 이상패턴 탐지 (이례성 신호 · 검토 우선순위 보조)

> **정의(현재).** 신규 사업의 정책적 정상·이상 여부를 판정하지 않고,
> 지원규모·지원건수·지원율·사업기간 등 정형 설계정보를 벡터화하여 유사사업
> 비교군의 대표 설계와 얼마나 떨어져 있는지를 계산한다. 이를 통해 과거
> 유사사업 대비 드문 설계 조합과 주요 차이축을 제시해 담당자의 검토
> 우선순위 설정을 보조한다.
>
> 서비스 문구는 `이상 사업 / 부적절 / 수정 필요`가 아니라 `유사사업 대비
> 이례적인 편 / 검토 우선순위 높음 / 관련 설계축 확인 필요`를 씁니다.

**상태: 구조는 재설계(M44) + 안정성 검증(M47~M49)까지 마쳤지만, 운영
배포는 아직 보류 — 경고 임계선이 없습니다.**

### 3.1 첫 버전(OneClassSVM)이 왜 무너졌는가

| 검증 기준 | recall | precision | ROC-AUC |
|---|---:|---:|---:|
| 합성 이상치 (튜닝 후) | 80.0% | 100% | — |
| 사람 라벨 50건 (M30, 실제 검증) | 0.33 | 0.25 | **0.48** |

ROC-AUC 0.48은 사실상 무작위 수준입니다. 합성 이상치는 "만들어 넣은
방향으로 잘 도는가"만 잴 뿐, 사람이 실제로 이상하다고 보는 것과는 다른
축이었습니다. 사람이 비전형이라 본 15건 중 8건은 설계가 드문 게 아니라
**값이 잘못 들어온 행**(파싱 오류 — 예: 사업기간 "26년"이 실은 공고연도
2026 오파싱)이었습니다.

### 3.2 구조를 다시 짠 결과 (M44, 최신)

비교군 대표벡터와의 거리(`||X − C||`)를 이상 점수로, 벗어난 방향은 점수에
안 넣고 설명 전용으로만 쓰는 구조로 재설계했습니다. **독립 라벨 53건**
(양성 5건)으로 **재튜닝 없이** 측정:

| 지표 | 값 |
|---|---:|
| ROC-AUC | **0.7542** [95% CI 0.575~0.908] |
| Recall / Precision (상위 20%) | 0.60 / 0.27 |
| 재표집 안정성(hold-out 상위11 유지율) | 0.918 |

M30의 0.48보다 크게 개선됐고 목록 안정성도 확보했지만 두 가지가 아직
막혀 있습니다 — **양성이 5건뿐이라 신뢰구간이 넓고**, 지금의 경고율 2%
임계선(pool 기준 0.980)을 이 53건에 그대로 적용하면 **경고가 0건**입니다.
임계선을 다시 잡으려면 점수가 아니라 **층화 추출된 별도의 운영
캘리브레이션 세트**가 필요합니다. 적용 범위도 전체의 59.9%(수치 축 2개
이상인 행)로 제한적입니다.

상세: `ml/reports/m30_m3_real_eval.md`(초기 붕괴 원인), `ml/reports/m44_m3_final_test.md`(재설계·최신 성능).

### 3.3 검증 방향 재정의 — 정답 대조에서 신호 안정성으로 (M47~M49)

3.2절의 53건·양성 5건은 **도메인 전문가(공무원·사전협의 담당자) Ground
Truth 가 아닙니다.** 이 라벨을 150~280건으로 늘려 ROC-AUC 를 재검증하려던
계획을 재검토했습니다 — 비전문가가 "이 사업이 행정적으로 비전형적인
설계인가"를 대규모로 판정하는 것은 신뢰성이 낮습니다. **대신 사람 라벨을
한 건도 쓰지 않고 잴 수 있는 것, 즉 이례성 신호 자체가 안정적인가를
검증했습니다.**

| 실험 | 검증 내용 | 핵심 결과 |
|---|---|---|
| M47 민감도 | 대표벡터·scaling 대안이 현행보다 나은가, 특정 feature 하나가 점수를 지배하는가 | mean·standard 유지 근거 확인. RobustScaler 는 ROC +0.0041 이지만 상위 30건 중 **67%를 다른 사업으로 교체** — ROC 하나만 보면 안 되는 근거 |
| M48 안정성 | 표본을 줄여도(재표집) 순위가 유지되는가, 비교군 최소표본수는 적절한가 | 표본 50%로 줄여도 순위상관 **0.94** 유지. `MIN_COHORT=20`(현행)이 균형점. 얇은 비교군 5~6개(`사업화×other` 29.4점 등)가 흔들림을 독점 |
| M49 Synthetic Stress Test | 설계 수치를 0.1~10배로 흔들었을 때 점수가 상식적으로 반응하는가 | 4축(지원액·기업수·기간·비율) 전부 **단조성 100%**. 다만 `기업당 지원액` 하나만 키워도 모델이 그 축을 최대 원인으로 지목하는 비율이 **56%**로 약함 — 방향 설명(4순위 과제)의 신뢰도 개선 여지 |

**53건의 ROC-AUC 0.7542는 폐기하지 않되 해석을 낮춥니다.** "제한된 사람
라벨셋에서 비교군 거리 점수와 사람의 비전형 판단 사이에 탐색적 정합성을
확인했다" 정도로만 읽습니다 — 최종 성능 인증이 아니라 **탐색적 보조
검증**입니다.

상세: `ml/reports/m47_m3_sensitivity.md`, `ml/reports/m48_m3_stability.md`,
`ml/reports/m49_m3_stress.md`.

---

## 4. 제외·보류된 후보 (참고용)

| 후보 | 결과 | 미채택 사유 |
|---|---|---|
| 월별 공고량 시계열 예측 | Ensemble MAE 21.45, baseline 대비 유의 | 성능은 확립됐으나 부처가 조정 근거로 쓸 유인·권한 약함 |
| 설계유형 군집 (부록A, 구 모델2) | grant 내부 6개 유형만 유효 | service·other는 기존 분야 분류를 다시 그린 것으로 판명 |
| Calibrated LinearSVM (모델 1) | 커버리지 70.7%에서 정확도 0.9164 | 보정에 학습데이터 30% 소모 — 무보정 LogReg(당시 기준선)를 못 넘음 |
| Autoencoder / Deep SVDD (모델 3) | 합성 이상치 recall 81.7%, ML보다 높음 | 재학습 시 상위 목록 유지율 29~48% — 표본에 따라 순위가 뒤집힘 |
| IsolationForest (설계서 MVP 권장) | 합성 이상치 회수율 8.3% | 결측 지시자·희귀 범주 빈도에 점수가 지배당함 |

---

## 5. 검증 방법 (공통 원칙)

- **그룹 K-Fold**: 같은 사업(`program_stem`)의 재공고가 학습/검증에 갈라지면
  최대 macroF1 0.025~0.26이 부풀려집니다. 전 모델 통제.
- **fold 내부 fitting**: TF-IDF·Scaler·결측 대치값을 fold train에만 적합.
- **외부/실제 검증셋**: CV·합성 벤치마크만으로 채택하지 않고, 모델 1은 외부
  Open API 131건, 모델 3은 사람 라벨 50건으로 재확인 — 둘 다 CV 단계와
  다른 결론이 나왔습니다.
- **정답이 불확실한 과제는 정답 대조 대신 신호 안정성을 검증**: 모델 3처럼
  "무엇이 정답인가" 자체에 비전문가 라벨의 신뢰도 문제가 있는 과제는, 라벨을
  늘려 정확도를 재검증하는 대신 재표집·민감도·synthetic stress test로
  **라벨 없이 신호가 안정적인지**를 검증합니다(3.3절).
