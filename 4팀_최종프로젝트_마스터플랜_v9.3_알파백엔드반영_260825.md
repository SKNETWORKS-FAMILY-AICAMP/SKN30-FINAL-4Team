# 4팀 최종 프로젝트 마스터플랜 v9.3 — 알파 백엔드 결정·의도 통합본

- 프로젝트: SK네트웍스 Family AI 캠프 30기 최종 프로젝트
- 갱신일: 2026-08-25 (**2026-09-02 알파 후 계약 갱신 반영**)
- 기준 문서: 마스터플랜 v9.2, 서비스 기획서 v0.3.1, CPL/FIT/SIM 판별기준, DB v2.1 문서·DDL, 알파 프론트 화면 초안, 알파 백엔드 구현 기준서 v0.1, 8월 19일 피드백, 8월 21일 주제 유지 결정, 8월 25일 사용자 확정사항, **9월 2일 프론트 협의 메모 2건**

> **2026-09-02 알림.** 알파 발표가 종료되어 최종 단계에 진입했다. 프론트와 직접 협의한 메모 2건(`프론트 메모.txt`, `프론트메모2.txt`)이 API 계약의 최우선 근거이며, 화면설계서와 API 초안은 뼈대로 강등된다. 충돌하면 메모를 따른다. 갱신된 계약은 `SIMS_API_프론트정합화_초안.md` v0.2에 있고, 이 문서에서는 10.4와 13.1이 영향을 받았다. 상세는 16.3 변경기록을 참조한다.

---

## 0. 이 문서의 기록 원칙

이 문서는 현재 결과뿐 아니라 **무엇이 확인됐고, 어떤 계기로 무엇이 바뀌었는지**를 인수인계하기 위해 작성한다.

이번 버전에는 다음 자료에서 직접 확인할 수 있는 내용만 기록한다.

| 표기 | 의미 |
|---|---|
| `[팀 결정]` | 사용자가 전달한 팀의 직접 결정 |
| `[피드백]` | 사용자가 전달한 발표·심사 피드백 |
| `[데이터 실측]` | 제공된 CSV를 직접 집계한 결과 |
| `[공식자료 확인]` | 확보된 공식 서식 또는 공식 출처에서 확인한 내용 |
| `[DB 문서]` | DB v2.1 SQL·상세설계서·논의정리에 명시된 내용 |
| `[파서 실험]` | `PARSING_OCR_PROGRESS_SUMMARY.md`에 기록된 실행·결과 |
| `[기획서]` | 서비스 기획서 v0.3.1의 Product Boundary와 요구사항 |
| `[사용자 확정]` | 사용자와의 질의응답에서 사용자가 직접 승인·선택한 사항 |
| `[알파 구현 결정]` | 2026-08-31 알파 프로토타입을 위해 확정한 구현 정책 |
| `[실험 기준]` | 공식·법적 기준이 아니라 알파 평가를 시작하기 위한 버전 관리 대상 초깃값 |
| `[구현 초안]` | 사용자가 방향을 승인한 백엔드 구조. 실제 코드·실행 완료와 구분 |
| `[현재 자료 미확인]` | 제공된 자료에서는 구현·확정 여부를 확인할 수 없는 내용 |
| `[프론트 협의 확정]` | 2026-09-02 알파 발표 후 프론트와 직접 협의해 확정한 API 계약 사항 |
| `[구현 확인]` | 실제 백엔드 코드를 읽어 확인한 동작. 문서가 아니라 코드가 근거 |

다음 내용은 이번 버전에서 제외한다.

- 팀 또는 사용자가 승인하지 않은 설계·평가·발표 전략
- 사용자에게 권고했으나 승인 여부가 확인되지 않은 안
- 실측하지 않은 성능·시장성·도입 가능성
- 제한된 표본의 결과를 전체 공고로 일반화한 표현

단, 이전 대화에서 제안으로 시작했더라도 사용자가 명시적으로 `권장안대로`, `그렇게 하자`라고 승인한 내용은 `[사용자 확정]` 또는 `[알파 구현 결정]`으로 기록한다. 공식 판정기준과 팀의 실험 기준은 반드시 구분한다.

### 0.1 v9.2에서 v9.3으로 갱신된 핵심

1. DB 기준을 v2.0에서 v2.1로 올리고 비밀번호 변경이력 정책을 반영했다.
2. 알파 발표일을 사용자가 말한 `다음 주 월요일` 기준 2026-08-31로 정정했다.
3. 서비스 기획서 v0.3.1의 CPL→FIT→SIM Product Boundary를 마스터플랜 기준으로 편입했다.
4. 프론트 화면 초안과 실제 확보 데이터의 차이를 정리했다.
5. CPL 확인율, FIT 상태·점수, SIM 검색·중복 검토 등급의 알파 산식을 확정했다.
6. Rule 우선·LLM 최소 사용, 결정적 오케스트레이터, 교체 가능한 멀티에이전트 구조를 확정했다.
7. 알파의 외부 GPT와 후속 로컬 sLLM의 관계를 확정했다.
8. HWP/HWPX, 파일 보존·삭제, PDF, 대화 이력, 로그인·비밀번호 변경 범위를 확정했다.
9. 단일 LLM·코사인 전용·규칙 기반 Baseline과 제안 구조를 같은 평가세트에서 비교하는 방향을 추가했다.
10. AI가 실제 코드를 작성할 때 의존성 붕괴를 방지하는 구현 순서와 테스트 원칙을 추가했다.

### 0.2 사용자가 이번 갱신에서 남기고자 한 의도 `[사용자 확정]`

사용자는 단순히 최신 구조도를 추가하는 것이 아니라 다음 세 가지를 다른 세션과 팀원에게 전달하고자 했다.

1. **무엇을 만들 것인가:** 기획서 v0.3.1을 기준으로 요청서 한 개를 누락·정합성·유사중복 관점에서 사전 점검하는 제품을 만든다.
2. **왜 이렇게 만들었는가:** Rule/SQL/코드로 가능한 부분에 LLM을 낭비하지 않고, 문맥 판단이 필요한 부분에만 LLM을 사용하며, 근거와 판단경계를 남기기 위해 CPL/FIT/SIM을 분리한다.
3. **앞으로 어떻게 바꿀 것인가:** 월요일 알파에서는 외부 GPT로 End-to-End를 빠르게 검증하지만, 특정 모델·파서·점수에 결합하지 않아 팀원 파서와 로컬 sLLM을 쉽게 교체하고 같은 평가세트로 비교한다.

사용자가 원하는 것은 `현재 동작하는 데모`에 그치지 않는다. 다른 세션에서 실제 코드를 작성할 때도 사용자 판단이 필요한 부분을 AI가 임의로 정하지 않고, 기능 하나와 테스트 하나를 함께 구현하여 국소 수정이 다른 기능을 깨뜨리는 문제를 줄이는 것이다.

---

## 1. 프로젝트 기본 정보

| 항목 | 내용 |
|---|---|
| 소속 | SK네트웍스 Family AI 캠프 30기 |
| 프로젝트 기간 | 2026-08-03 ~ 2026-09-18 |
| 수료식 | 2026-09-23 |
| 팀원 | 김도훈, 김동휘, 김효선, 서해연, 이동욱 |
| 지정 기본 틀 | 대화형 데이터 분석 에이전트 및 자동 리포팅 플랫폼 |

### 발표 일정

| 구분 | 날짜 | 확인된 상태 |
|---|---|---|
| 기획 발표 | 2026-08-13 | 완료 |
| 중간 발표 | 2026-08-19 | 완료, 피드백 수령 |
| 스탠드업 | 2026-08-27 | 예정 |
| 알파 발표 | 2026-08-31 | 예정, 50~70% 수준 프로토타입 목표 |
| 스탠드업 | 2026-09-10 | 예정 |
| 베타 발표 | 2026-09-14 | 예정 |
| 최종 발표 | 2026-09-18 | 예정 |

---

## 2. 현재 주제와 범위

### 2.1 현재 주제 `[팀 결정]`

중소기업지원사업 사전협의 제출 전에 사업담당자가 제출자료를 점검하는 사전협의 Pre-review.

### 2.2 MVP 핵심 기능 `[팀 결정·기획서]`

1. 기존 지원사업과의 유사·중복 가능성 리포팅
2. 사전협의 제출자료의 필수 작성항목 누락 체크

서비스 기획서 v0.3.1은 위 두 기능을 다음 검토 흐름으로 구체화한다.

```text
사전협의 요청서 HWP/HWPX 1개
→ CPL: 요청자료 완전성·기초구조 점검
→ FIT: 현재 사업안 내부 정합성 점검
→ SIM-R: 기존사업 후보 검색·축소
→ SIM-1~4: 목적·대상·지원내용·수행체계 비교
→ SIM-S: 검토 우선순위와 비교근거
→ BEN: 명시적으로 확인 가능한 중복수혜 관련 참고정보
→ DIF: 기존사업 정보 대비 확인된 차이
→ Evidence Trace
→ Pre-review 검토요약서와 결과 기반 대화
```

누락기능은 단순히 빈칸만 세는 기능이 아니다. CPL에서 요청서 항목의 존재·식별·기초구조를 확인하고, FIT에서 추출된 항목 사이의 최소 연결성·범위 차이·명시적 충돌을 점검하는 `문서점검` 기능으로 구현한다.

### 2.3 출력·판단 경계 `[기획서·사용자 확정·DB 문서]`

- 공고를 유사·중복 Retrieval의 비교 단위로 사용한다.
- 기본 Top-K는 5이며 실행별 실제 값을 저장한다.
- 누락검사와 유사·중복검사 결과를 하나의 확정 리포트로 저장하도록 설계했다.
- 리포트 PDF 다운로드와 결과 기반 대화 이력 저장 구조가 DB 문서에 포함돼 있다.
- 확정된 결과는 수정하지 않고 재검사 시 새 검사 건을 생성하도록 설계했다.
- 코사인 유사도는 후보검색 지표이며 중복 확률이 아니다.
- 시스템은 중복사업, 실제 중복수혜, 법적·정책적 타당성을 확정하지 않는다.
- 사용자 출력은 `검토 우선순위 + 항목별 비교 + 원문 근거`를 중심으로 한다.
- 정보가 부족하거나 일부 분석이 실패해도 가능한 분석은 계속 수행한다.

위 항목은 기획서·사용자 결정·DB 설계를 통합한 현재 기준이다. 실제 전체 기능 구현과 실행 완료를 의미하지 않는다.

### 2.4 알파와 최종제품의 관계 `[사용자 확정]`

프론트 화면과 사용자 흐름은 기획서 v0.3.1을 기준으로 만든다. 알파는 기획서와 다른 축소 제품이 아니라 같은 제품의 첫 구현 단계다.

- 프론트: 기획서 기준의 화면 흐름과 결과 구조를 설계
- 알파 백엔드: 동작 가능한 초기 규칙·점수·외부 GPT로 핵심 흐름 연결
- 후속 백엔드: 파서, 규칙, 프롬프트, 임계값, LLM을 평가하며 교체

따라서 알파 이후 주로 바뀌어야 하는 것은 백엔드 내부 분석 정밀도다. 단, 프론트와 합의한 API 응답 구조·상태·결과 의미는 가능한 한 유지한다.

### 2.5 사용자가 지키고자 한 설계 원칙 `[사용자 확정]`

```text
코드로 확정 가능한 부분은 Rule/SQL/일반 코드
문맥 판단과 결정이 필요한 부분은 LLM
단계 간 상호작용과 실패 처리는 에이전트·오케스트레이션
```

이 원칙의 목적은 단순한 비용 절감만이 아니다.

- 같은 입력에 같은 결과를 내는 부분은 재현 가능하게 한다.
- LLM이 만들어낸 값에는 원문 근거를 요구한다.
- 모델을 바꿀 때 전체 백엔드를 다시 작성하지 않는다.
- Rule을 LLM으로 또는 LLM을 Rule로 바꿀 수 있다.
- 공공기관 운영 단계에서는 모든 LLM 기반 에이전트를 동일 로컬 sLLM로 교체할 수 있다.

---

## 3. 주요 변경 이력과 확인된 이유

### 3.1 8월 13일: 기존 주제에서 Pre-review로 변경

- **변경 전:** 이전 프로젝트 주제를 진행했다.
- **변경 후:** 사전협의 제출 전 점검으로 주제를 전환했다.
- **현재 자료에서 확인되는 배경:** 마스터플랜 v8.2에는 이전 주제에서 주제 확정 후 데이터를 검증한 점을 실패 원인으로 기록하고 있으며, 새 주제에서는 기업마당 데이터와 공식 서식을 확인하는 과정을 진행했다.
- **기록 한계:** 이번에 제공된 자료만으로 당시 팀원별 의사결정 발언 전체를 재구성하지 않는다.

### 3.2 8월 18일: DB v1.0에서 v2.0으로 보강 `[DB 문서]`

DB 논의정리 v2.0에 기록된 직접 원인은 다음과 같다.

1. 기업마당 공고의 `공고문 참조` 문구가 임베딩 본문에 섞이는 문제
2. `announcement_embedding`과 `chunk_embedding`에 실제 `input_text`가 없어 과거 벡터 입력문을 재현할 수 없는 문제
3. 같은 임베딩 프로필에 다른 차원의 벡터가 들어갈 수 있는 문제
4. 사용자 파일과 검사 건 소유권이 교차 연결될 수 있는 문제
5. 삭제 아웃박스에 기록된 객체 저장 키가 재사용될 수 있는 문제
6. HNSW 예시 쿼리의 실행 형태 보강 필요

v2.0에서 확인되는 변경은 다음과 같다.

- 테이블 36개와 뷰 2개는 유지
- 명시적 인덱스 33개 → 55개
- 트리거 4개 → 11개
- 함수 3개 → 7개
- 명명된 제약 98개 → 108개
- `COMMENT ON` 0개 → 12개
- `announcement_embedding.input_text`, `chunk_embedding.input_text` 추가
- `detail_ref_fields`, `has_detail_ref` 추가
- 임베딩 차원 검증, 사용자 파일 소유권 제약, 저장 키 재사용 방지 보강

**검증 상태:** DB 문서에는 정적 검사를 완료했다고 기록돼 있다. 같은 문서에 PostgreSQL 서버가 없어 실제 DB 실행 검증은 완료하지 않았다고 명시돼 있다.

### 3.3 검색 데이터 범위 변경 `[팀 결정·DB 문서]`

- **이전 기록:** 마스터플랜 v8.2에서는 중앙부처 909건과 기업마당 1,570건을 모두 적재·검색하는 결정을 기록했다.
- **현재 DB v2.1 문서:** 기업마당 Open API의 현재 공고만 Retrieval에 사용하고, 기업마당 과거 약 9만 건과 중앙부처 909건은 보관 전용이며 검색·임베딩에서 제외한다고 기록돼 있다.
- **확인된 이유:** 과거 자료에는 종료일이 없거나 현재 모집 여부를 구분하기 어렵고, 현재 설계는 공고를 비교 단위로 사용한다는 팀 논의가 기록돼 있다.
- **현재 기준:** 최신 구현 기준은 DB v2.1 문서의 정책이다.

### 3.4 8월 19일: 중간발표 피드백 수령 `[피드백]`

중간발표에서 확인된 지적은 다음 세 범주다.

1. 범용 기업용 솔루션과의 차별성 및 정부 데이터 POC 범위
2. 공공기관의 외부 LLM 사용 제한, 로컬 LLM 성능, 오판 책임과 보안
3. 세 에이전트의 핵심 기능, 단일 에이전트 대비 멀티에이전트의 필요성, 성능·응답시간·비용 평가

구체적인 피드백 내용은 4장에 별도로 보존한다.

### 3.5 8월 20일: HWP/HWPX/PDF 파서·OCR 사례 검증 `[파서 실험]`

- 중소기업 기술혁신개발사업 HWP 직접 파싱
- 혁신파트너십 공고 HWP와 PDF Pair Test
- 관악구 융자 공고 PDF와 HWPX 비교
- 관악구 PDF 1~3쪽에 대해 pdf-inspector OCR, PaddleOCR, EasyOCR, Surya 비교

실험 방법과 결과는 7장에 정리한다.

### 3.6 8월 21일: 주제 유지 결정 `[팀 결정]`

사용자가 전달한 팀 결정은 다음과 같다.

> 현재 피벗하기에는 메리트가 없고 데드라인이 얼마 남지 않았기 때문에 리스크는 안고 간다. 다만 우리만의 스토리를 보강해 설득 위주로 가며, 할 수 있는 만큼 진행한다.

이 결정에서 직접 확인되는 이유는 두 가지다.

1. 현재 시점의 피벗 메리트가 없다고 팀이 판단했다.
2. 데드라인이 얼마 남지 않았다.

다음 내용은 팀 결정으로 확인되지 않았으므로 이 문서에서 확정하지 않는다.

- 구체적으로 어떤 스토리를 사용할지
- 어떤 리스크 대응책을 구현할지
- 어떤 기능을 최우선으로 개발할지
- 멀티에이전트 평가를 어떤 실험군으로 진행할지

### 3.7 8월 21일: XLSX 첨부 현황 추가 확인 `[데이터 실측]`

- `fileNm`에서 XLSX 71개 확인
- XLSX 71개는 66개 공고에 연결
- 해당 66개 공고는 모두 별도의 대표 파일 `printFileNm` 보유
  - PDF 39건
  - HWP 19건
  - HWPX 5건
  - PNG 2건
  - JPG 1건
- 파일명에는 참가신청서, 제품설명서, 단가표, 기술 목록, 산업분류표, 사업비 산출내역서 등이 함께 존재

**현재 설계와의 차이:** DB v2.1 문서가 명시한 상세 첨부 지원 형식은 HWP, HWPX, PDF, PNG, JPG, DOCX다. XLSX는 지원 형식 목록에 포함돼 있지 않다. 따라서 XLSX 파싱·검색 반영 방식은 현재 확정된 설계로 기록하지 않는다.

### 3.8 8월 21일: CPL/FIT/SIM 판별기준 초안 정리 `[기획서·팀 문서]`

- CPL은 요청서에서 어떤 정보를 식별·구조화할지 13개 주요 영역으로 정리했다.
- FIT은 같은 요청서에서 추출된 항목 사이의 7개 내부 관계를 어떻게 대조할지 정리했다.
- SIM은 후보검색과 목적·대상·지원내용·수행체계의 구조 비교를 분리했다.
- 해당 문서의 세부 판별 기준은 법령이 직접 정한 자동판정 산식이 아니라 공식 자료를 참고해 팀이 만든 초기 검토기준이다.

### 3.9 8월 21~25일: 유사와 중복의 의미 분리 `[사용자 확정]`

사용자는 유사·중복을 하나의 코사인 점수로 합치지 않기로 했다.

- **유사:** 코사인 유사도 Top-K와 후보 분포·밀집도를 실험하여 검색 후보를 찾는다.
- **중복 가능성:** 목적·지원대상·지원내용·수행체계가 실제로 얼마나 겹치는지 구조적으로 비교한다.
- **사용자 출력:** 코사인 점수와 중복 검토 등급을 분리한다.

이 결정의 이유는 문구가 유사해도 대상 집합이나 지원수단이 다를 수 있고, 표현이 달라도 실질적인 지원대상·목적·내용이 겹칠 수 있기 때문이다.

### 3.10 8월 25일: 알파 백엔드 구현정책 확정 `[알파 구현 결정]`

- HWP/HWPX 1개, 최대 50MB
- 원본·추출값·분석결과·PDF·대화는 사용자가 분석 건을 삭제할 때까지 보관
- UI 상태는 분석 중·분석 완료·분석 실패 세 가지
- 부분 실패는 가능한 섹션을 결과로 제공하고 실패 섹션만 판단 불가 처리
- CPL 확인율, FIT 상태·점수, SIM 4축 가중치의 알파 초깃값 확정
- 기업마당 현재 공고 Top-5와 원문 근거 제공
- 승인·거절 사례 데이터가 없으므로 해당 라벨과 문구 미사용
- 결과 기반 Chat과 동일 `report_json` 기반 PDF 제공
- 회원가입·비밀번호 분실은 제외하고 데모 계정 로그인과 현재 비밀번호 기반 변경만 구현
- 발표 노트북 한 대에서 프론트·FastAPI·PostgreSQL/pgvector·로컬 파일저장소 실행
- 알파 모델은 `gpt-4o-mini`, 임베딩은 `text-embedding-3-small`
- OpenAI API 키를 보유하고 있으며 알파 예산 상한은 300달러, 세부 호출한도는 미정

### 3.11 8월 25일: 교체형 구현과 테스트 전략 확정 `[사용자 확정·구현 초안]`

사용자는 이전 프로젝트에서 AI가 한 부분을 고칠 때 의존 모듈이 연쇄적으로 어긋나는 문제를 겪었다. 이를 반복하지 않기 위해 다음 방식을 채택했다.

1. 전체 흐름과 설계 이유를 먼저 문서화한다.
2. 파서·LLM·임베딩·저장소·작업실행기·PDF를 공통 인터페이스로 분리한다.
3. 기능을 수직 단위로 나눈다.
4. 기능 하나와 테스트 하나를 함께 구현하고 즉시 전체 흐름에 연결한다.
5. 매 단계 관련 테스트와 전체 회귀 테스트를 실행한다.
6. 사용자 판단이 필요한 변경은 AI가 임의로 결정하지 않고 질문한다.

---

## 4. 8월 19일 중간발표 피드백

### 4.1 황용철 위원 `[피드백]`

- 중소기업 심사 과정의 인력 의존과 반복 비용 문제는 다른 기업체에도 존재할 수 있다고 지적했다.
- 현재 설명만으로는 일반 기업용 솔루션과의 차이가 잘 보이지 않는다고 지적했다.
- AI의 역할이 사업 분석·문제 해석 수준으로만 설명돼 솔루션 고유 특징이 약하다고 지적했다.
- 정부 사업 POC라면 정부 데이터로 테스트해야 하며, 정부 사업이 실제 타깃인지와 데이터 확보 범위를 질문했다.

### 4.2 윤세일 위원 `[피드백]`

- 정부 관련 프로젝트에서는 ChatGPT·Gemini 등 외부 모델을 사용할 수 없는 경우가 있다고 지적했다.
- 로컬 LLM 사용 시 성능이 낮을 수 있다고 지적했다.
- 잘못된 판단이 예산상 불이익으로 이어질 수 있어 책임 문제가 있다고 지적했다.
- 보안과 책임에 대한 시스템이 없으면 완성도가 높아도 실제 사용이 어려울 수 있다고 지적했다.

### 4.3 안현준 강사 `[피드백]`

- 세 에이전트의 핵심 기능과 성능평가 기준을 질문했다.
- 단일 에이전트나 단순 챗봇으로 할 수 없는 이유가 있어야 한다고 지적했다.
- 응답시간과 비용도 함께 검토해야 한다고 지적했다.

### 4.4 피드백 이후 확인된 팀 입장 `[팀 결정]`

- 주제를 유지한다.
- 현재 리스크를 안고 간다.
- 팀의 스토리를 보강해 설득 중심으로 진행한다.
- 남은 기간에 할 수 있는 만큼 진행한다.

### 4.5 피드백 이후 확정된 기술 대응 `[사용자 확정·알파 구현 결정]`

8월 21일 당시에는 구체적 기술 대응이 미확정이었지만 8월 25일 기준 다음 대응이 정해졌다.

| 피드백 | 현재 대응 |
|---|---|
| 범용 LLM과의 차별성 | 기획서 기반 CPL/FIT/SIM 검토구조, 기업마당 grounding, Evidence Trace, 출력통제 |
| 외부 LLM 보안 | 알파는 비민감 실험문서로 외부 GPT를 사용하고 후속 운영가정은 로컬 sLLM |
| 오판·책임 | 최종판정 금지, 검토 우선순위·정보부족·원문근거 표시 |
| 멀티에이전트 필요성 | 책임·입출력·평가단위를 CPL/FIT/SIM/Chat으로 분리하고 오케스트레이터는 코드 상태머신으로 구현 |
| 단일 LLM 대비 효과 | 동일 평가세트에서 규칙·단일 LLM·코사인 전용·제안 구조 비교 |
| 비용·응답시간 | Rule 우선, LLM 최소 호출, 토큰·지연·비용 기록 및 모델별 비교 |

이 대응은 실제 성능 검증이 끝났다는 뜻이 아니다. 알파 구현과 후속 평가를 위한 팀의 현재 방향이다.

---

## 5. 확보 데이터와 실측 결과

### 5.1 사전협의 서식 `[공식자료 확인]`

- 중기부 사전협의 지침의 `【서식 1】` 요청서 원본 PDF를 확보했다.
- 문서 기준연도는 2024년도다.
- 기존 화면 기반 복원본과 대조했을 때 필수 기재항목과 조건부 기재규칙이 일치했다고 마스터플랜 v8.2에 기록돼 있다.
- 사업설명자료 작성 항목과 제출자료 자체 점검표 원본은 현재 제공 자료에서 확인되지 않는다.

### 5.2 현직자 인터뷰 `[인터뷰 1명]`

- 인터뷰 참여자 1명은 사전협의 작성 시 SIMS에서 수작업하며 AI 기능을 사용하지 않는다고 답했다.
- 한 명의 진술이므로 전체 기관·사용자로 일반화하지 않는다.

### 5.3 기업마당 Open API 표본 `[데이터 실측]`

- 분석 레코드: 1,570건
- `bsnsSumryCn`을 HTML 제거·공백 정규화 후 처음 두 `☞`를 기준으로 목적·대상·내용으로 분할
- 해당 표본에서 목적·대상·내용 3분할 결과 결측 0건으로 기록
- 지원대상에 `공고문 참조` 포함: 247건, 15.73%
- 지원내용에 `공고문 참조` 포함: 457건, 29.11%
- 하나 이상 포함: 602건, 38.34%
- 참조문구 제거 후 지원대상 완전 공백: 0건
- 참조문구 제거 후 지원내용 완전 공백: 0건

### 5.4 과거 데이터 `[데이터 실측·DB 문서]`

- 중앙부처 2023년 상반기 자료: 실제 데이터 행 909건
- 기업마당 과거 목록 자료: 약 9만 건, 제공 분석본은 97,794건으로 마스터플랜 v8.2에 기록
- 현재 DB v2.1 정책: 두 과거 자료 모두 보관 전용이며 검색·임베딩 제외

### 5.5 `fileNm` 첨부 확장자 `[데이터 실측]`

집계 기준은 `fileNm`을 `@`로 분해한 후 각 파일명의 마지막 확장자를 세는 방식이다. `.pdf.zip` 1건은 ZIP으로 분류한다.

| 확장자 | 첨부파일 수 |
|---|---:|
| HWP | 997 |
| PDF | 329 |
| HWPX | 190 |
| ZIP | 166 |
| XLSX | 71 |

추가로 기록된 공고 레코드 기준 수치는 다음과 같다.

- HWP/HWPX 보유 공고: 881건
- PDF 보유 공고: 266건
- HWP/HWPX와 PDF 모두 보유: 157건

위 수치는 첨부파일 발생 수와 공고 수를 구분해 사용한다.

---

## 6. DB v2.1에 확인된 설계와 알파 적용

### 6.1 기술과 데이터 범위 `[DB 문서]`

| 항목 | 문서에 명시된 내용 |
|---|---|
| DBMS | PostgreSQL 15+ |
| 벡터 저장·검색 | pgvector 0.7+ |
| 별도 벡터 DB | 사용하지 않음 |
| GraphDB | MVP에서 구축하지 않음 |
| 현재 공고 소스 | 기업마당 Open API |
| 과거 자료 | 보관 전용, 검색·임베딩 제외 |
| 기본 Top-K | 5 |
| 사용자 업로드 | 검사 1건당 대표 문서 1개 |
| 사용자 업로드 형식 | HWP, HWPX, PDF |
| 상세 첨부 지원 형식 | HWP, HWPX, PDF, PNG, JPG, DOCX |
| 공고 동기화 | KST 날짜 기준 하루 한 번 |
| 검색 상태 | `OPEN`, `UNKNOWN` 후보, 확인된 `CLOSED` 제외 |

DB v2.1은 사용자 업로드 저장 구조상 PDF까지 수용하지만, 8월 25일 알파 제품 입력은 기획서에 맞춰 HWP/HWPX 1개로 좁혔다. 공고 첨부 파싱의 PDF·이미지 지원과 사용자 직접 입력 형식을 혼동하지 않는다.

### 6.2 파일·파싱·청킹·임베딩 저장 구조 `[DB 문서]`

| 저장 대상 | 주요 DB 구조 |
|---|---|
| 파일 메타데이터 | `file_asset` |
| 공고와 첨부 연결 | `announcement_attachment` |
| 파싱 실행·전체 텍스트·구조 | `document_parse_run` |
| 청킹 설정 | `chunking_profile` |
| 청크 실행 묶음 | `document_chunk_set` |
| 실제 청크와 원문 위치 | `document_chunk` |
| 청크 임베딩 | `chunk_embedding` |
| 후보 검색 실행·후보 | `retrieval_run`, `retrieval_candidate` |
| 비교 근거 | `candidate_evidence` |
| 확정 리포트 | `inspection_report` 관련 구조 |

### 6.3 DB와 애플리케이션 코드의 경계 `[DB 문서]`

- DB는 파서 제품·라이브러리를 확정하지 않았다.
- DB는 OCR 엔진을 확정하지 않았다.
- DB는 임베딩 모델·차원을 확정하지 않았다.
- DB는 파싱·청킹 결과와 실행 이력을 저장하도록 설계돼 있다.
- 실제 파서 실행, 청킹, 임베딩 생성은 애플리케이션 구현이 필요하다.

### 6.4 현재 검증 상태 `[DB 문서]`

- DDL 정적 검사 결과가 문서에 기록돼 있다.
- 빈 PostgreSQL 15+ 환경에서의 전체 DDL 실행 검증은 완료되지 않았다고 기록돼 있다.
- HNSW 실제 인덱스는 운영 모델·차원 확정 후 생성하도록 문서화돼 있으며 현재 통합 스키마에 실제 인덱스가 포함됐다고 기록돼 있지 않다.

### 6.5 v2.0에서 v2.1로 변경된 사항 `[DB 문서]`

v2.1은 v2.0의 공고·파싱·임베딩·검사 구조를 유지하고 비밀번호 변경 보안 이력을 추가한 신규 설치용 통합 DDL이다.

| 항목 | v2.1 |
|---|---:|
| 테이블 | 37개 |
| 조회 뷰 | 2개 |
| 명시적 인덱스 | 56개 |
| 트리거 | 12개 |
| 함수 | 8개 |

추가된 핵심은 다음과 같다.

- `app_user.password_changed_at`
- 성공한 비밀번호 변경만 기록하는 `password_change_history`
- 비밀번호 해시 변경과 이력 기록을 한 트랜잭션에서 수행하는 함수·트리거
- 백엔드가 토큰 발급시각과 `password_changed_at`을 비교하여 기존 세션을 무효화하는 정책

회원가입과 비밀번호 분실 이메일 재설정은 알파에서 구현하지 않으므로 별도 재설정 토큰 테이블은 추가하지 않는다.

### 6.6 프론트 화면 지표와 v2.1의 관계 `[사용자 확정·알파 구현 결정]`

DB v2.1은 자체 점검도·구조적 정합성·통합 쟁점·과거 선례의 제품 요구사항이 확정되지 않아 전용 테이블을 만들지 않았다. 8월 25일에 화면 지표의 의미는 정해졌지만 알파에서는 다음처럼 처리한다.

- CPL/FIT/SIM 점수는 원천 결과에서 계산 가능한 파생값이므로 산식 버전과 함께 불변 `report_json`에 저장
- 검토 쟁점은 CPL/FIT/SIM 결과를 화면용으로 집계하여 `report_json.review_issues`에 저장
- 사용자의 쟁점 수용·보류·거절 기능이 없으므로 `review_issue` 정규화 테이블은 아직 추가하지 않음
- 승인·거절 선례 데이터가 없으므로 `prior_review_precedent`를 추가하지 않음
- FIT 전용 실행 테이블은 알파에서 추가하지 않고 FIT-1~7 결과·근거·버전을 `report_json`에 고정

후속 운영에서 점수 교차통계, 부분 재실행, 사용자 피드백 이력이 필요해질 때 정규화 테이블을 검토한다.

---

## 7. 파서·OCR 실험에서 확인된 사실

### 7.1 실험 범위 `[파서 실험]`

전체 결과는 다음 두 공고 사례를 중심으로 한다.

1. 혁신파트너십 공고 1건
2. 관악구 중소기업육성기금 융자지원 공고 1건

파서 진행 요약은 결과를 전체 HWP/HWPX/PDF 공고로 일반화하지 말라고 명시한다.

### 7.2 HWP/HWPX 결과 `[파서 실험]`

- `rhwp-python`으로 HWP 텍스트, IR, Canonical JSON을 생성했다.
- 테스트 문서에서 문단·표·셀·병합셀·문단 순서를 비교적 안정적으로 보존했다.
- 관악구 HWPX에서 7×3 융자절차 표와 제한조건 표를 읽었다.

### 7.3 PDF native extraction 결과 `[파서 실험]`

- 혁신파트너십 Pair Test에서 일반 텍스트 대부분을 얻었으나 일부 표·도식 구조와 읽기 순서가 손실됐다.
- 관악구 PDF는 텍스트 객체와 이미지 텍스트가 혼재했다.
- 제목 등 일부 텍스트 객체가 있어 자동 OCR 대상에서 제외됐지만 1~3쪽 핵심 본문은 native text로 충분히 복원되지 않았다.

### 7.4 OCR 비교 결과 `[파서 실험]`

관악구 PDF 1~3쪽의 `35억`, `1억원`, `5천만원`, `1.5%`, `6개월`과 융자절차 도식을 비교했다.

| 경로 | 기록된 결과 |
|---|---|
| pdf-inspector 내장 OCR | 한국어 본문·숫자 품질이 낮았음 |
| PaddleOCR | 본문·조건 다수 복원, `1.5%`를 `15%`로 읽은 사례 발생 |
| EasyOCR | `1.5%`, `35억`, `5천만원`, `6개월` 복원, `1억원`→`1엄원`, `융자`→`움자` 오류 발생 |
| Surya | 핵심 숫자·조건 복원, 기본 경로에서 Diagram 블록이 skip됨 |
| Surya Diagram fallback | 자동 탐지한 Diagram bbox를 재처리해 융자절차 단계와 기관 관계 복원 |

PaddleOCR의 네 가지 옵션 실험에서는 처리시간이 51.138초~72.833초였으며, 단순 해상도·이진화 조정으로 품질이 안정적으로 개선되지 않았다.

EasyOCR은 CPU에서 페이지당 약 10.4~10.9초가 기록됐다.

Surya 실험 환경은 RTX 4090 24GB, Surya 0.22.1, vLLM 0.20.1+cu129였다. 기본 전체 페이지 순차 처리 wall time은 1쪽 209초, 2쪽 103초, 3쪽 100초로 기록됐고 1쪽에는 최초 import/cache 지연이 포함됐다. Diagram fallback은 `max_tokens=4096`에서 21.995초로 기록됐다.

### 7.5 현재 확정하지 않는 내용

다음은 제공 자료만으로 팀의 최종 결정 여부를 확인할 수 없어 확정안으로 쓰지 않는다.

- 운영 파서 제품·라이브러리
- OCR 기본 엔진과 fallback 순서
- Canonical JSON의 최종 DB 계약
- 청킹 방법과 토큰 크기·중첩값
- XLSX 파서 도입 여부
- 전체 공고 대상 파싱 성공률

---

## 8. 현재 확인된 상태와 미확인 상태

| 영역 | 확인된 상태 | 아직 구현·검증되지 않은 상태 |
|---|---|---|
| 주제 | Pre-review 유지 결정 | 장기 BM·실제 구매자 검증 |
| MVP 기능 | 기획서 v0.3.1의 CPL→FIT→SIM 흐름과 출력경계 확정 | 전체 기능 구현 완료·실측 성능 |
| 데이터 | 공식 요청서, Open API 표본, 과거 자료, 첨부 확장자 실측 | 실제 작성 완료 요청서 표본 |
| DB | v2.1 SQL·설계 문서, 37개 테이블·2개 뷰, 정적 검사 기록 | 실제 PostgreSQL 전체 실행 검증·알파 HNSW migration |
| 파서 | 사용자 요청서는 HWP/HWPX 텍스트·표, 공고 첨부는 OCR 가능 구조로 역할 분리 | 팀원 파서의 최종 공통 계약 연결·전체 성공률 |
| 청킹 | DB 저장 구조 존재 | 청킹 코드·파라미터 확정 및 실행 결과 |
| XLSX | 71개·66개 공고·대표 공고문 별도 존재 확인 | 현 DB 지원, 파싱·검색 반영 정책 |
| 멀티에이전트 | 결정적 사실은 Rule·개방적 의미 판단은 LLM·최종 검증은 코드, CPL/FIT/SIM/Report/Chat 책임분리, 코드 오케스트레이터 확정 | 실제 구현과 단일 LLM 대비 평가 |
| 모델 | 알파 `gpt-4o-mini`, 임베딩 `text-embedding-3-small`; 후속 로컬 sLLM 교체 원칙 | 호출량·비용·로컬 후보·성능 |
| 점수 | CPL/FIT/SIM 알파 산식 v0.1 확정 | 사람 라벨 기반 보정과 공식 현장 적합성 |
| 보안·책임 | 최종판정 금지, Evidence, 비민감 알파문서, 모델교체 경계 확정 | 실제 기관 배포 보안·망·감사 정책 |
| 프론트 | 업로드·진행·결과·PDF·Chat 화면 초안 존재 | 최종 API field명과 E2E 연결 |
| 테스트 | 계층·Baseline·수직 구현 원칙 확정 | 팀 평가자료와 실제 테스트 실행 결과 |
| 발표 스토리 | 법·공식 workflow를 최대한 구현한 근거기반 Pre-review라는 사용자 의도 확정 | 구체적인 발표 슬라이드와 실측 결과 |

> 2026-08-29 정정: 위 `멀티에이전트` 행의 원문은 `Rule 우선, CPL/FIT/SIM/Report/Chat 책임분리, 코드 오케스트레이터 확정`이었다. `Rule 우선`이 "코드로 만들 수 있으면 의미 판단도 Rule로 구현한다"는 일반 우선순위로 해석되어 열린 의미 분류까지 키워드·정규식으로 처리하는 편향을 만들었으므로, 실제 확정 내용인 수단 분담 서술로 바꿨다. 책임분리와 오케스트레이터 확정 사항 자체는 그대로다. 근거: `docs/Pre-review_CPL_FIT_SIM_계약_정정기록_v0.1_260829.md` CR-01.

---

## 9. 현재 기준 파일

| 파일 | 역할 |
|---|---|
| `4팀_최종프로젝트_마스터플랜_v8.2_260815.md` | 이전 마스터플랜 및 8월 15일까지의 정정 이력 |
| `4팀_최종프로젝트_마스터플랜_v9.2_팩트기반_260821.md` | 본 버전 직전의 사실·변경이력 기준 |
| `중소기업 지원사업 사전협의 Pre-review 서비스 기획서 v0.3.1.pdf` | Product Boundary, CPL/FIT/SIM/BEN/DIF, Evidence, 대화·요약서 요구사항 |
| `중소기업지원사업_PreReview_CPL_판별기준_정리본_20260821.md` | CPL 13개 구조화·상태·판단경계 |
| `중소기업지원사업_PreReview_FIT_판별기준_정리본_20260821.md` | FIT-1~7 관계·판단경계 |
| `중소기업지원사업_PreReview_SIM_판별기준_정리본_20260821.md` | SIM-R, 4축 구조비교, 검토 우선순위 |
| `sims_mvp_schema_v2.1.sql` | 최신 신규설치용 통합 DB DDL |
| `SIMS_MVP_DB_상세설계서_v2.1_260821.md` | DB 테이블·제약·워크플로우 상세 |
| `SIMS_MVP_DB_설계_논의정리_v2.1_260821.md` | v1.0→v2.0→v2.1 변경 원인과 보류사항 |
| `SIMS_MVP_SIM_유사중복_판별및_교체형구현설계_v0.1_260821.md` | 유사·중복 기준과 교체형 코드 설계 |
| `SIMS_PreReview_알파_백엔드_구현기준서_v0.1_260825.md` | 알파 범위, 18개 ADR, API·테스트·구현 인수인계 |
| 프론트 화면 초안 4종 | 업로드·분석중·결과·Chat 목표 화면 |
| `PARSING_OCR_PROGRESS_SUMMARY.md` | HWP/HWPX/PDF·OCR 사례 실험 기록 |
| 기업마당 Open API CSV | 1,570건 분석 및 첨부 확장자·XLSX 확인 근거 |
| 중앙부처 2023 상반기 CSV | 실제 데이터 행 909건 |

---

## 10. 알파 화면과 백엔드 계약 `[사용자 확정·알파 구현 결정]`

### 10.1 업로드·분석 진행

- HWP와 HWPX를 모두 받는다.
- 요청서 한 개의 최대 크기는 50MB다.
- 업로드 후 백엔드가 검사 건을 만들고 분석 작업을 시작한다.
- 프론트에는 `분석 중 / 분석 완료 / 분석 실패`만 표시한다.
- 내부 DB는 파싱·점검·검색·리포트 등 상세 상태를 유지한다.

문서 파싱 자체가 실패한 경우 전체 `분석 실패`로 처리한다. 파싱 후 일부 분석 모듈만 실패한 경우 가능한 결과는 `분석 완료`로 제공하고 실패한 섹션을 `판단 불가`와 경고로 표시한다.

이렇게 정한 이유는 한 항목의 정보 부족이나 외부 API 오류가 전체 Pre-review를 불필요하게 중단시키지 않게 하기 위해서다.

### 10.2 분석 이력

- 이력 제목은 요청서에서 추출한 사업명이다.
- 사업명을 추출하지 못하면 원본 파일명을 사용한다.
- 원본·추출결과·리포트·PDF·대화는 사용자가 분석 이력을 삭제하기 전까지 보관한다.
- 분석 건을 삭제하면 사용자 소유 하위자료를 함께 삭제한다.
- 공유 공고 데이터는 사용자 분석 삭제로 제거하지 않는다.

프론트 초안의 `분석 완료 후 즉시 파기` 문구는 분석 이력·PDF 재다운로드·Chat 이력과 충돌하므로 사용하지 않는다.

#### 2026-09-02 갱신 `[프론트 협의 확정]`

| 항목 | 확정 내용 |
|---|---|
| 이력 제목 | **업로드한 파일명.** 요청서에서 사업명을 추출하는 기능은 **만들지 않는다** |
| 이력에 담는 것 | **완료된 분석만.** 진행 중·실패 건은 넣지 않으므로 상태 필드가 필요 없다 |
| 진행 중 분석 | 프론트가 업로드 시 받은 `case_id`를 저장해 복구한다. 새로고침해도 대기 화면이 유지된다 |
| 실패한 분석 | 결과 화면으로 넘어가지 않고 업로드 화면에서만 알린다 |
| 표시 시각 | **완료 시각으로 통일.** 이력 카드와 결과 화면 Header 모두 |
| 페이지네이션 | 최초 5건, 더보기마다 5건씩 누적. 커서 기반이며 **조회 상한 없음** |
| 이력 삭제 | **기능을 두지 않는다.** 화면설계서에 해당 화면이 없다 |
| 재시작으로 끊긴 분석 | **서버가 뜰 때 실패로 정리한다.** 분석이 프로세스 안의 태스크로만 돌기 때문에 새로 뜨는 순간 이전 진행 건은 반드시 유실된 상태다. 그대로 두면 화면이 영원히 분석 중에 머문다. 정리는 기동을 막지 않으며, 서버를 여러 개 띄우면 `SWEEP_INTERRUPTED_ANALYSES_ON_STARTUP=false` 로 끄고 시간 기준 정리로 바꾼다 |

### 10.3 결과 화면

| 화면 블록 | 실제 의미 |
|---|---|
| 자체 점검도 | CPL 13개 주요 영역 중 확인된 영역 수와 비율 |
| 구조적 정합성 | FIT-1~7 중 판단 가능한 관계의 가중평균과 판단 가능 수 |
| 검토 쟁점 | CPL/FIT/SIM에서 확인된 `검토 필요` 항목과 근거 |
| 유사 프로그램 | 기업마당 현재 공고 Top-5와 의미적 유사도·중복 검토 등급 |
| 상세 보기 | 목적·대상·지원내용·수행체계의 공통점·차이점·원문 근거 |
| 보고서 | 같은 `report_json`으로 생성한 PDF |
| AI 질의응답 | 현재 요청서·결과·Top-5 근거 범위의 후속 질문 |

프론트 초안의 `46개 중 42개`, `평균 대비 +5%`, `승인됨/거절됨`, `과거 승인 문구`는 현재 근거 데이터가 없으므로 실제 화면값으로 사용하지 않는다.

#### 2026-09-02 갱신 `[프론트 협의 확정]`

| 응답 필드 | 화면 이름 | 확정 내용 |
|---|---|---|
| `cpl` | **요청자료 완전성·기초구조 점검** | 확인 수는 `13개 중 8개` 형태 유지, **퍼센트 제거**. 항목별 상태를 그대로 내려보내고 화면 표현은 프론트가 정한다. **요청 유형 체크박스 표시는 통째로 제거**한다 |
| `fit` | **내부 정합성 점검** | **점수를 제공하지 않는다.** 판단 가능한 관계 수와 관계별 상태·요약·근거만. 관계는 코드로만 주고 이름은 프론트가 갖는다 |
| `similar_candidates` | 유사 프로그램 | 5건, **순위·유사도 점수 제거**, 공고 원문 링크 유지, 4축 비교와 원문 근거 유지 |
| — | 검토 쟁점 | **제외.** AI 질의응답으로 대체한다 |

Header는 **파일명 + 완료 시각**이다. 목업의 `문서 ID: PRG-2024-883A` 형식 식별자는 만들지 않는다.

**중복수혜 문구**를 유사 후보마다 제시한다. 공고문에서 `중복 수혜 불가` 같은 문구를 **키워드로 찾아 원문 문장을 그대로 보여주며, 중복 여부를 판정하지 않는다.** 후보 5건 전부를 검사하고, 근거가 있으면 표시하고 없으면 표시하지 않는다.

**SIM 재료를 공고문 파싱으로 전환한다.** 현재는 기업마당 API 요약 텍스트를 쓰는 알파 상태이며, 최종은 공고문 첨부를 다운로드·파싱한 결과를 쓴다. 파싱 대상은 `attachment_role = 'PRIMARY'`(공고문 본문)뿐이고 `AUXILIARY`(신청 서식)는 보지 않는다. 팀원 작업 완료 후 연결한다.

전환 근거는 현재 공고 1,532건 전수 실측이다. HWP/HWPX 보유 1,242건(81.1%), PDF만 보유 261건(17.0%), 이미지·ZIP 등만 29건(1.9%), **첨부 없음 0건**. 첨부를 읽지 못한 후보는 제외하고 다음 순위로 채워 항상 5건을 유지한다.

### 10.4 API 외형 고정 원칙

파서·Rule·LLM·임계값이 바뀌더라도 프론트 API의 핵심 외형은 유지한다.

제품 내부 로직을 교체하기 쉽도록 만들면서도 프론트가 매번 다시 작성되지 않게 하는 것이 사용자의 의도다.

#### 알파 기준 외형

```text
case
ui_status
self_check
structural_consistency
review_issues
similar_candidates
ben_references
differences
warnings
report_download_url
schema_version
```

#### 2026-09-02 갱신 `[프론트 협의 확정]`

**위 알파 외형은 화면이 확정되기 전에 만든 것이다. 프론트 협의를 기준으로 화면용 응답을 새로 짠다.**

단, **바꾸는 것은 화면용 투영(`ReportResponse`)뿐이다.** 내부 스냅숏(`ReportJsonV01`)은 그대로 둔다. PDF 렌더러가 내부 스냅숏을 읽고, 평가·Baseline 비교(14장)도 그 원자료를 쓰기 때문이다. 보고서가 화면보다 자세한 것은 문제가 아니며, PDF를 화면과 맞추는 정리는 필요해지면 별도로 한다.

화면이 쓰지 않아 화면용 응답에서 제외하는 항목은 다음과 같다.

| 제외 항목 | 사유 |
|---|---|
| `review_issues` 전체 | 검토 쟁점은 AI 질의응답으로 대체한다 |
| `self_check.confirmation_rate` | 화면에 확인율 퍼센트를 쓰지 않는다 |
| `similar_candidates[].rank` | 화면에 순위를 쓰지 않는다 |
| `similar_candidates[].relevance_score` | 화면에 유사도 점수를 쓰지 않는다 |

네 항목 모두 다른 값에서 계산해내는 **파생 값**이다. 검토 쟁점은 CPL·FIT·SIM 결과에서 조립되고, 확인율은 확인 수를 13으로 나눈 값이며, 순위는 정렬 결과다. 화면에서 빼도 판단 정보를 잃지 않는다. 네 항목 모두 내부 스냅숏에는 그대로 남아 PDF와 평가에 계속 쓰인다.

추가 변경은 다음과 같다.

- 공통 4필드 래퍼(`code`·`message`·`data`·`errors`)를 성공 응답에서 제거한다.
- `self_check`와 `structural_consistency`의 화면 명칭을 바꾼다. 새 이름은 미정이다.
- 유사 프로그램 후보는 5건으로 고정한다.
- 4축 비교와 원문 근거는 그대로 유지한다. 근거 표시에 별도 API를 호출하지 않는다.
- 분석 이력과 분석 결과에 분석 시작 시각을 넣는다.
- 분석 이력 상태에 진행 중을 포함한다.

10.3 결과 화면 표의 `검토 쟁점` 행은 화면 블록에서 제외되며, 해당 판단은 AI 질의응답이 담당한다.

---

## 11. 알파 분석 기준 `[사용자 확정·실험 기준]`

### 11.1 CPL — 요청자료 완전성·기초구조

현재 요청서에서 다음 13개 주요 영역을 식별한다.

1. 요청유형 체크값
2. 사업 목적·목표
3. 연차별·내역사업별 추진계획
4. 사업기간
5. 신설·변경 주요내용
6. 사업필요성 최소 논리구조
7. 지원근거
8. 연계정책
9. 사업예산
10. 지원대상·지원조건
11. 지원내용·지원규모
12. 수행기관·수행방식·수행체계
13. 기대효과·성과 관련 정보

화면의 퍼센트는 문서 품질이나 사업 타당성 점수가 아니다.

```text
CPL 확인율 = 확인된 주요 영역 수 / 13 × 100
```

항목별 상태와 추출내용·근거가 본문이고 퍼센트는 요약이다.

### 11.2 FIT — 현재 요청서 내부 정합성

| ID | 관계 |
|---|---|
| FIT-1 | 사업목적 ↔ 지원대상 |
| FIT-2 | 사업목적 ↔ 지원내용 |
| FIT-3 | 사업목적 ↔ 기대효과·성과지표 |
| FIT-4 | 세부사업 ↔ 내역사업/내내역사업 |
| FIT-5 | 지원대상 ↔ 지원조건 |
| FIT-6 | 수행기관 ↔ 역할·수행절차 |
| FIT-7 | 지원내용 ↔ 지원규모의 수치·조건 일관성 |

관계별 상태와 알파 점수는 다음과 같다.

```text
적합      100
검토 필요  50
불일치      0
판단 불가  점수 계산에서 제외
```

알파에서는 일곱 관계의 가중치를 동일하게 둔다. 화면에는 퍼센트와 함께 `판단 가능 관계 n/7개`를 표시한다.

여기서 `적합`은 해당 관계에서 최소 연결성이 확인됐다는 의미이며, 사업 전체의 정책적 적합성을 인정한다는 뜻이 아니다. 동일 가중치와 점수는 사람 라벨을 확보하기 전의 실험 기준 v0.1이다.

### 11.3 유사사업 Retrieval

- 비교자료: 기업마당 Open API의 현재 공고
- 후보상태: `OPEN`, `UNKNOWN`; 확인된 `CLOSED` 제외
- 임베딩: `text-embedding-3-small`
- 검색: pgvector 코사인 거리 Top-5
- 표시: 코사인 유사도를 0~100 범위의 `의미적 유사도 점수`로 변환

코사인 점수는 중복 확률이 아니다. Threshold와 유사 밀집도는 실제 평가자료로 실험한 뒤 조정하며 알파 화면의 필수값으로 고정하지 않는다.

### 11.4 중복 가능성 구조 비교

Top-5 후보를 다시 비교한다.

| 축 | 알파 가중치 |
|---|---:|
| 사업목적 | 30% |
| 지원대상 | 30% |
| 지원내용 | 30% |
| 수행체계 | 10% |

축별 상태는 `유사 100 / 일부 유사 50 / 상이 0 / 판단 불가 산식 제외`로 둔다.

```text
70 이상          중점 검토
40 이상 70 미만  일반 검토
40 미만          낮은 우선순위
비교근거 부족     판단 보류
```

Top-5는 모두 유사사업 후보로 표시하고 `중점 검토·일반 검토`를 중복 가능성 검토대상으로 집계한다. 이 값은 공식 행정기준이 아니라 알파 실험 기준 `sim-alpha-v0.1`이다.

### 11.5 쟁점과 Evidence Trace

- 근거가 없는 `심각 T1/T2`, 높음·중간·낮음 심각도는 사용하지 않는다.
- 사용자 표시는 `검토 필요`로 통일한다.
- 첫 화면에는 짧은 근거와 위치를 보여준다.
- 상세보기에는 원문 excerpt, 페이지·절·표 셀, 추출방식·버전을 제공한다.
- 동일 개념이 여러 위치에서 다르게 나오면 대표값을 임의 선택하지 않고 occurrence를 보존한다.

### 11.6 BEN·DIF 판단경계

- BEN: 공고 원문에서 명시적으로 확인된 중복지원·참여제한 조건만 참고정보로 제공
- DIF: SIM 비교에서 확인된 차이를 요약
- 금지: 실제 중복수혜 위험점수, 충분한 차별화 판정, 중복사업 확정

---

## 12. 알파 백엔드와 멀티에이전트 구조 `[사용자 확정·구현 초안]`

### 12.1 실행구성

```text
발표 노트북 한 대
├─ Frontend
├─ FastAPI 모듈러 모놀리스
├─ PostgreSQL 15+ / pgvector 0.7+
├─ 로컬 파일 저장소
└─ OpenAI API
   ├─ gpt-4o-mini
   └─ text-embedding-3-small
```

처음부터 마이크로서비스·Redis·Celery를 모두 도입하지 않는다. 월요일 알파의 배포·디버깅 비용을 낮추되 모듈과 인터페이스는 분리하여 후속 확장이 가능하게 한다.

### 12.2 멀티에이전트 책임

| 구성요소 | 책임 | 주 실행방식 |
|---|---|---|
| Analysis Orchestrator | 단계 순서·상태·부분실패·재현성 | 코드 상태머신 |
| CPL Analyzer | 13개 영역 추출·누락·근거 | Rule + LLM |
| FIT Analyzer | FIT-1~7 관계 판단 | Rule + LLM |
| Retrieval Module | 임베딩·pgvector Top-5 | 코드/SQL |
| SIM Analyzer | 후보별 4축 비교·검토등급 | Rule + LLM |
| Report Composer | 결과 통합·금지표현 통제 | 코드 우선, 제한적 LLM 요약 |
| Result Chat Agent | 결과 기반 후속질문 | LLM + 제한된 Evidence |

오케스트레이터를 LLM에 맡기지 않는다. 실패처리와 단계 순서는 코드로 재현한다. 멀티에이전트의 목적은 LLM 호출 수를 늘리는 것이 아니라 판단 책임과 평가단위를 분리하는 것이다.

### 12.3 Rule과 LLM의 경계

Rule·SQL·코드 우선:

- 파일 검증, 소유권, 상태전이
- 체크박스·표 위치, 날짜·금액·비율 형식
- 필수값 존재 여부
- 코사인 검색과 점수 산식
- 등급 구간과 결과 병합

LLM 사용:

- 목적·필요성·수행체계 의미 구조화
- FIT 관계의 문맥 연결·충돌 판단
- SIM 목적·대상·내용·수행체계 축별 비교
- 근거 기반 설명·요약·후속 질의응답

### 12.4 교체 가능한 경계

다음 포트를 도메인 로직과 분리한다.

```text
DocumentParser
LLMClient
EmbeddingClient
ObjectStorage
JobDispatcher
PdfRenderer
```

사용자 요청서는 OCR 없이 텍스트·표를 파싱한다. 기업마당 공고 첨부는 별도 파서에서 OCR을 사용할 수 있다. 다른 팀원의 파싱 로직이 확정되면 `DocumentParser` 구현체만 바꾼다.

알파에서는 모든 LLM 기반 분석기가 같은 `gpt-4o-mini` 프로필을 사용한다. 후속 공공기관 배포 가정에서는 같은 계약을 구현하는 로컬 sLLM 프로필로 교체한다.

LLM과 임베딩은 분리한다. LLM만 바꾸면 기존 벡터를 유지할 수 있지만 임베딩 모델을 바꾸면 공고 전체 재임베딩·인덱스 재생성·threshold 재보정이 필요하다.

---

## 13. 인증·파일·리포트·대화 정책 `[사용자 확정]`

### 13.1 인증

- 회원가입 없음
- 기관이 발급했다는 컨셉의 데모 계정 로그인
- 알파에서 비밀번호 분실·이메일 재설정 제외
- 로그인 후 현재 비밀번호를 확인하여 새 비밀번호로 변경
- 성공 변경만 DB 이력 저장
- 변경 전 발급 토큰 무효화
- 후속 이메일 재설정은 Gmail SMTP, 링크 10분, 해시 토큰, 1회 사용을 후보정책으로 유지
  (2026-09-03 정정: 링크 대신 임시 비밀번호 발급으로 확정. 16.6 참조)

#### 2026-09-02 갱신 `[프론트 협의 확정]`

이메일 재설정은 후보정책이 아니라 **구현 완료 상태**이며 최종 범위에 포함한다.

| 항목 | 확정 내용 |
|---|---|
| 로그인 식별자 | **이메일**. 임시 이메일을 DB에 직접 투입한다 |
| 로그인 조회 컬럼 | **`app_user.email`.** 비밀번호 재설정도 같은 컬럼을 보므로 한쪽으로 로그인하고 다른 쪽으로 재설정 메일이 가는 상황이 생기지 않는다. `login_id` 컬럼에 이메일을 넣어 맞추려던 안은 실제 데이터가 달라 정정했다 |
| 토큰 무효화 기준 | **토큰에 발급 당시의 `password_changed_at`을 담고 DB 의 현재 값과 같은지만 본다.** 두 시각의 앞뒤를 비교하지 않는다. `password_changed_at`은 DB 시계로, 발급 시각은 앱 시계로 찍히는데 실측에서 DB 가 0.1~0.4초 앞서 있었고, 그 차이를 발급 시각으로 메우면 JWT 라이브러리가 미래 토큰(약 0.39초 초과)으로 보고 거부했다. 두 범위가 겹쳐 부하에 따라 결과가 갈렸다 |
| 사용자 표시 이름 | **`app_user.display_name` 을 쓰고, 비어 있으면 이메일의 `@` 앞부분을 대신 쓴다.** 어느 쪽이든 전체 주소는 응답에 담지 않는다. 응답 필드는 `name` 하나로 그대로다 (2026-09-04 정정, 16.7 참조) |
| 프로필 사진 | 프론트 고정 아이콘. 사용자별 사진 기능을 두지 않는다 |
| 세션 수명 | **1시간** (알파 30분에서 상향) |
| 세션 연장 | **명시적 연장 엔드포인트.** 요청마다 자동 연장하는 슬라이딩 방식이 아니다 |
| 연장 UI·남은 시간 계산 | 프론트가 전부 처리한다. 프론트가 토큰의 `exp`를 직접 읽는다 |
| 로그아웃 | **API 자체를 두지 않는다.** 서버가 토큰을 폐기하지 않기로 해 아무 일도 하지 않는 껍데기였다. 프론트가 저장 토큰을 삭제하며, 해당 토큰은 만료까지 유효하다 |
| 시도 횟수 제한 | **로그인·비밀번호 변경에 도입하지 않는다.** 재설정 요청 제한만 유지한다 |
| `401` 원인 구분 | 하지 않는다 |
| 재설정 방식 | **임시 비밀번호를 메일로 보낸다.** 링크·토큰·확인 API 를 두지 않는다 (아래) |

**세션 30분이 알파에서 로그인 풀림 증상의 원인이었다.** 토큰 발급 지점이 로그인 하나뿐이고 갱신 수단이 없어, 만료 시각이 로그인 순간 확정된 뒤 어떤 호출로도 늘어나지 않았다. 분석·결과 열람·질의응답을 이어가면 30분을 넘긴다.

재설정은 임시 비밀번호를 만들어 메일로 보내고 사용자가 그 값으로 로그인한다. 12자에 영문·숫자·특수문자를 각각 한 자 이상 담아 비밀번호 정책을 확정적으로 만족하며, 사람이 옮겨 적으므로 `0/O`·`1/l/I` 는 쓰지 않는다. **메일을 보낸 뒤에 비밀번호를 바꾼다.** 먼저 바꾸고 발송이 실패하면 아무도 모르는 값으로 계정이 잠기기 때문이다. 계정 존재 비노출, 요청 횟수 제한, SMTP TLS, 응답 `no-store` 는 그대로다.

**받아들인 위험이 하나 있다. 이메일 주소만 알면 남의 계정을 잠글 수 있다.** 요청하는 순간 비밀번호가 교체되므로 메일함을 못 보는 제3자도 계정 주인의 로그인을 막을 수 있다. 링크 방식에는 없던 성질이며, 로그인 영역에 힘을 주지 않기로 한 판단에 따라 감수한다. 메일함에 임시 비밀번호가 평문으로 남는 것도 같은 이유로 감수하고, 변경을 강제하지 않는다.

남는 위협은 이메일 계정 자체의 탈취다. 별도 채널 2차 인증 없이는 어떤 서비스도 막을 수 없어 범위에서 제외한다.

화면설계서 별첨의 `등록되지 않은 이메일 주소입니다` Alert은 계정 존재 여부를 노출하므로 **구현하지 않는다.**

### 13.2 파일과 삭제

- 원본은 사용자 검사 건에 연결
- 파일명 대신 안전한 storage key 사용
- 분석 삭제 시 DB 하위자료와 물리 파일 삭제를 모두 추적
- OpenAI API 키·DB 비밀번호는 환경변수로 관리하고 Git에 저장하지 않음
- 외부 GPT 알파에는 개인정보·민감정보가 제거된 실험 문서만 사용

### 13.3 PDF

백엔드가 확정 `report_json`으로 PDF를 생성·저장한다. 프론트 화면과 PDF는 같은 데이터 원천을 사용하며 사용자가 분석을 삭제하기 전까지 재다운로드할 수 있다.

### 13.4 Chat

알파 답변 범위는 현재 요청서, 현재 분석결과, Top-5 공고·첨부 근거다. 법령·내부문서·과거 승인사례는 데이터가 없어 확장 목표로 남긴다. 대화는 검사 건별로 저장하고 리포트 결과를 수정하지 않는다.

---

## 14. 검증·Baseline·테스트 계획 `[사용자 확정·구현 초안]`

### 14.1 왜 Baseline이 필요한가

사용자는 `왜 Rule·LLM·코드 검증을 나눈 멀티에이전트 구조를 선택했는가`를 결과로 증명하고자 한다. 즉 결정적 사실 추출·정규화·검증은 Rule이 맡고, 표현이 개방적이고 문맥 해석이 필요한 의미 판단은 LLM이 맡으며, 최종 신뢰성은 코드가 Evidence와 결과 계약을 검증해 확보하는 구조가 단일 LLM보다 나은가를 본다. 동일 모델과 동일 평가세트에서 다음을 비교한다.

> 2026-08-29 정정: 이 문장의 원문은 ``사용자는 `왜 Rule 우선 멀티에이전트 구조를 선택했는가`를 결과로 증명하고자 한다``였다. 구현 계약은 `Rule 우선`을 폐기하고 수단 분담으로 정정했는데 증명하려는 명제만 옛 표현으로 남으면, 구현과 평가 논지가 서로 다른 주장을 하게 된다. 비교 실험군과 지표는 바꾸지 않았고 증명 대상 명제만 실제 구조에 맞췄다. 근거: `docs/Pre-review_CPL_FIT_SIM_계약_정정기록_v0.1_260829.md` CR-01.

| 실험군 | 설명 |
|---|---|
| B0 | Rule·키워드 기반 |
| B1 | 단일 LLM이 전체 분석·리포트 생성 |
| B2 | 코사인 Top-5와 점수만 제공 |
| P | 결정적 사실은 Rule, 개방적 의미 판단은 LLM, Retrieval + CPL/FIT/SIM 분리 + 코드의 Evidence·계약 검증 |

> 2026-08-29 정정: 실험군 P의 원문 설명은 `Rule + Retrieval + CPL/FIT/SIM 분리 + Evidence Trace`였다. 14.1이 증명하려는 명제를 수단 분담으로 고친 뒤에도 P 설명만 Rule이 앞장서는 표현으로 남으면, 명제와 실험군이 서로 다른 구조를 가리킨다. P가 실제로 구현하는 시스템과 B0~B2 비교 대상, 14.2 지표는 바뀌지 않았고 P가 무엇인지에 대한 서술만 실제 구조에 맞췄다. 근거: `docs/Pre-review_CPL_FIT_SIM_계약_정정기록_v0.1_260829.md` CR-01.

### 14.2 평가 지표

- CPL 필드 추출 정확도와 누락 탐지율
- FIT 관계별 사람 라벨 일치와 False Positive
- Retrieval Recall@5
- SIM 축별 라벨 일치
- 중점 검토 후보 Precision/Recall
- Evidence Coverage와 원문 위치 확인 성공률
- JSON 계약 준수율
- 평균 응답시간·토큰·비용
- 부분실패 시 유효결과 생성률

### 14.3 테스트 구축 원칙

```text
Unit
Contract
PostgreSQL+pgvector Integration
API
End-to-End
Model Evals
```

일반 테스트에서는 실제 OpenAI 호출을 Fake/Mock으로 대체하고 모델 품질 평가는 별도 eval로 실행한다. PostgreSQL·pgvector 통합테스트를 SQLite로 대체하지 않는다.

### 14.4 알파 평가자료 `[미확정]`

팀과 협의하여 정상 HWP, 정상 HWPX, 일부 항목 누락 요청서, 명백히 유사한 공고, 대상이 배타적인 후보, 정보부족 후보를 만든다. 각 사례에는 CPL 기대상태, FIT 관계상태, SIM 축별 사람 라벨을 남긴다.

---

## 15. 구현 순서와 다음 행동

### 15.1 수직 구현 순서 `[사용자 확정·구현 초안]`

1. 프로젝트 골격·설정·공통 오류
2. 빈 PostgreSQL에 v2.1 DDL 적용 검증
3. 데모 인증·비밀번호 변경
4. 파일 업로드·소유권·로컬 저장소
5. Parser 계약과 임시 HWP/HWPX 구현
6. CPL 13개·누락·Evidence
7. FIT-1~7·점수
8. 기업마당 동기화·공고 버전
9. 임베딩·pgvector Top-5
10. SIM 4축 비교·검토등급
11. 통합 `report_json`
12. PDF 생성·저장
13. 결과 기반 Chat
14. 이력·삭제
15. 전체 E2E·Baseline·평가 하네스

### 15.2 AI 코딩 원칙 `[사용자 확정]`

- 백엔드 전체 맥락과 설계 이유를 먼저 읽는다.
- 구현 전 변경 파일·의존 모듈·API/DB 영향·테스트를 제시한다.
- 사용자 판단이 필요한 부분을 임의로 결정하지 않는다.
- 기능 하나와 테스트 하나를 함께 구현한다.
- 기능을 마지막에 한꺼번에 합치지 않고 매 단계 즉시 통합한다.
- 국소 테스트와 전체 회귀 테스트를 모두 실행한다.
- Rule·threshold·weight·prompt·model을 하드코딩하지 않는다.
- 외부 provider 응답 객체를 도메인 계층으로 누출하지 않는다.

### 15.3 알파 직전 필수 확인

1. 팀원 파서의 최종 입출력을 `ParsedDocument` 계약에 연결
2. 팀 평가용 요청서·기대결과 확정
3. 프론트 최종 report JSON field명 합의
4. OpenAI timeout·retry·호출한도 설정
5. 임베딩 프로필의 실제 차원에 맞는 partial HNSW migration
6. Happy Path와 파싱 실패 E2E 실행
7. 발표 노트북에서 프론트·백엔드·DB·PDF·Chat 전체 시연

---

## 16. 이후 변경기록 작성 규칙

### 16.1 v9.2 → v9.3 변경기록

```text
[2026-08-25]
- 이전 결정: 멀티에이전트 역할·평가안, 피드백 기술대응, 알파 분석 산식이 미확정
- 새 결정: CPL/FIT/SIM 분석 기준, Rule/LLM 경계, 교체형 멀티에이전트, 알파 모델·화면·인증·보존·PDF·Chat·테스트 정책 확정
- 변경 계기 또는 확인된 근거: 서비스 기획서 v0.3.1, 팀 판별기준 문서, 프론트 화면 초안, DB v2.1, 사용자 질의응답
- 사용자가 직접 밝힌 변경 이유: 월요일 알파에서 50~70% 프로토타입을 완성하고, 이후 파서·규칙·로컬 sLLM을 쉽게 교체·평가하며, 다른 코딩 세션이 의도를 임의로 바꾸지 않게 하려는 목적
- 영향을 받는 문서·기능: 마스터플랜, 알파 백엔드 기준서, API 계약, 프론트 결과 의미, DB report_json, 테스트·평가
- 아직 결정하지 않은 항목: 팀원 파서 최종 계약, 평가자료, threshold·밀집도 보정, 로컬 sLLM 후보, 실제 운영 보안
```

### 16.2 후속 변경 양식

앞으로 팀에서 결정을 추가하거나 변경할 때만 다음 양식으로 갱신한다.

```text
[날짜]
- 이전 결정:
- 새 결정:
- 변경 계기 또는 확인된 근거:
- 팀이 직접 밝힌 변경 이유:
- 영향을 받는 문서·기능:
- 아직 결정하지 않은 항목:
```

팀이 직접 밝히지 않은 이유는 대신 작성하지 않는다. 데이터·실험 결과에서 직접 확인되는 내용은 수치와 표본 범위를 함께 기록하며, 제안은 마스터플랜의 확정 이력과 분리한다.

### 16.3 알파 후 API 계약 갱신

```text
[2026-09-02]
- 이전 결정: 알파 API 외형(10.4)과 인증 정책(13.1)이 화면설계서·알파 구현 기준으로 확정됨.
  이메일 재설정은 후보정책, 세션 30분, 검토 쟁점을 화면 블록으로 포함
- 새 결정: 프론트 협의 메모 2건을 API 계약의 최우선 근거로 승격.
  화면이 쓰지 않는 응답 항목(검토 쟁점·확인율·순위·유사도 점수) 제거,
  공통 4필드 래퍼 제거, 오류 문구 소유를 프론트로 이관,
  세션 1시간과 명시적 연장 엔드포인트, 로그인·비밀번호 변경 시도 제한 미도입,
  이메일 재설정을 최종 범위로 편입하고 보안은 현행 유지,
  업로드 즉시 분석, 이력 커서 페이지네이션과 진행 중 상태 포함
- 변경 계기 또는 확인된 근거: 알파 발표 종료 후 프론트와 직접 협의.
  알파에서 보고된 로그인 풀림 증상의 원인이 세션 30분 고정과 갱신 수단 부재임을 코드에서 확인
- 팀이 직접 밝힌 변경 이유: 화면설계서와 API 초안은 구현 전에 그린 뼈대일 뿐이며,
  프론트와 직접 나눈 협의가 최종 기준이라는 판단
- 영향을 받는 문서·기능: 마스터플랜 10.3·10.4·13.1, SIMS_API_프론트정합화_초안 v0.2,
  백엔드 응답 스키마와 인증, Swagger 문서
- 아직 결정하지 않은 항목: 진행 상태 알림 방식(롱폴링·SSE), 사용자 영역 표시가
  사람 이름인지 역할인지, 비밀번호 변경 후 새 토큰 발급 여부, 로그인 요청 필드명,
  이메일을 넣을 DB 컬럼, token_type 유지 여부, CPL·FIT 새 명칭, 결과 화면 식별자,
  경과 시간의 서버 시각 전달 방식, 재설정 링크 경로 분리, 결과 대시보드 항목·배치,
  프론트 메모 중 뜻이 불분명한 3건(CPL 요약·옵션 제거 범위, 해당 없음 표시, 중복수혜 판정)
```

### 16.4 화면설계서 전수 대조 완료

```text
[2026-09-02]
- 이전 결정: 프론트 메모를 최우선 근거로 승격하고 3~9페이지까지 확정
- 새 결정: 화면설계서 16페이지 전수 대조 완료.
  진행 상태를 롱폴링으로 확정, 경과 시간 미표시, 결과 Header는 파일명+완료 시각,
  CPL·FIT 화면명과 응답 필드명 확정, FIT 점수 미제공, 요청유형 체크박스 표시 제거,
  중복수혜는 키워드로 원문 문구만 제시하고 후보 5건 전부 검사,
  이력은 완료 건만 담아 상태 필드 제거, 이력 상한·삭제 없음,
  비밀번호 변경 응답에 새 토큰 포함, 대화는 최근 20개 + 커서 더보기,
  SIM 재료를 공고문 PRIMARY 첨부 파싱으로 전환,
  경로는 전부 현행 유지하고 Swagger 검토 때 일괄 재정리
- 변경 계기 또는 확인된 근거: 화면설계서 페이지별 대조, 라우트·동기화 코드 확인,
  기업마당 공고 1,532건 첨부 현황 전수 조회
- 팀이 직접 밝힌 변경 이유: 로그인 영역에 힘을 주지 않고 최소 기능으로 가되
  사용자가 빠져나오지 못하는 흐름은 만들지 않는다는 판단.
  화면설계서 목업 그림은 참고용이며 프론트 협의가 최종 기준이라는 판단
- 영향을 받는 문서·기능: 마스터플랜 10.2·10.3·10.4·13.1,
  SIMS_API_프론트정합화_초안 v0.4, 백엔드 전 라우트, Swagger
- 아직 결정하지 않은 항목: 결과 응답 최종 JSON 형태, 경로 구조 재정리,
  SIM 공고문 파싱 연결(팀원 작업 대기)
```

### 16.5 계약 구현과 Swagger 정리

```text
[2026-09-02]
- 이전 결정: 화면설계서 전수 대조로 계약을 확정한 상태
- 새 결정: 확정한 계약을 백엔드에 구현하고 Swagger 를 사용 설명서로 정리.
  공통 응답 래퍼 제거, 세션 1시간과 명시적 연장 엔드포인트, 이메일 로그인,
  업로드 즉시 분석과 롱폴링, 이력을 완료 건만 담는 커서 페이지네이션,
  분석 상세 통합, 결과 응답을 화면이 쓰는 것만 남기기,
  경로에서 확장자와 한 단계 제거, 태그를 인증·분석 둘로 통합
- 변경 계기 또는 확인된 근거: 실제 계정 데이터에서 login_id 와 email 이 달라
  로그인 조회 컬럼을 email 로 정정했다. 앱과 DB 시계를 실측해 DB 가 0.1~0.4초
  앞선 것을 확인하고, 비밀번호 변경 직후 발급한 토큰이 거부되던 문제를 고쳤다.
  기업마당 공고 1,532건 첨부 현황을 전수 조회해 98.1% 가 HWP·HWPX·PDF 임을
  확인했다.
- 팀이 직접 밝힌 변경 이유: 화면 문구의 주인은 프론트이며 계약의 핵심은
  상태 코드라는 판단. 스웨거가 곧 사용 설명서라는 판단
- 영향을 받는 문서·기능: 10.2·10.3·10.4·13.1, 백엔드 전 라우트,
  SIMS_API_프론트정합화_초안 v0.6
- 아직 결정하지 않은 항목: SIM 공고문 파싱 연결(팀원 작업 대기)
```

API 는 14개에서 13개로 줄었다. 분석 시작·로그아웃·보고서 조회가 사라지고 세션 연장이 생겼으며, 보고서 조회는 분석 상세에 합쳐졌다.

#### 해소된 위험 `[구현 확인]`

진행 중 분석이 서버 재시작을 넘기지 못한다. 분석 작업이 인메모리 태스크라 서버가 죽거나 재배포되면 유실되고, 해당 검사 건은 완료도 실패도 되지 않은 채 진행 중 상태로 남는다. 프론트 협의의 "분석은 지속 가능하게끔", "진행 표시를 계속 보여주게끔"과 직접 충돌한다. 브라우저 새로고침은 해당하지 않으며 서버 재시작만 문제다.

### 16.6 비밀번호 재설정 방식 변경

```text
[2026-09-03]
- 이전 결정: 재설정 링크를 메일로 보내고, 프론트가 만든 재설정 화면에서
  토큰과 새 비밀번호로 확인 API 를 호출한다. 보안은 현행 유지
- 새 결정: 임시 비밀번호를 만들어 메일로 보내고 사용자가 그대로 로그인한다.
  확인 API, 재설정 토큰, PASSWORD_RESET_URL 설정을 없앤다.
  메일을 보낸 뒤에 비밀번호를 바꾼다
- 변경 계기 또는 확인된 근거: 링크 방식은 프론트에 재설정 화면 하나를
  요구하는데 임시 비밀번호 방식은 화면이 늘지 않는다. 백엔드도 455줄 줄었다.
  이메일 주소만으로 남의 계정을 잠글 수 있다는 점을 확인하고 감수했다
- 팀이 직접 밝힌 변경 이유: 로그인 영역에 힘을 주지 않기로 한 결정
- 영향을 받는 문서·기능: 13.1, SIMS_API_프론트정합화_초안 v0.9,
  이메일 비밀번호재설정 연동계약, Swagger, 기동 로그, .env
- 아직 결정하지 않은 항목: SIM 공고문 파싱 연결(팀원 작업 대기)
```

API 는 13개에서 12개로 줄었다.

> **2026-09-04 정정.** 이 절의 결정은 이후 되돌렸다. 재설정은 다시 메일 링크와 확인 API 를 쓰고 인증 경로 이름도 바뀌었다. 현행은 `docs/Pre-review_프론트_API_계약_v1.0_260904.md` 를 따른다.


### 16.7 사용자 표시 이름 컬럼 추가

```text
[2026-09-04]
- 이전 결정: 사용자 표시 이름은 이메일의 @ 앞부분을 서버가 잘라서 보내고,
  app_user 에 이름 컬럼을 추가하지 않는다
- 새 결정: app_user 에 display_name 컬럼을 두고 그 값을 쓴다. 비어 있으면
  종전대로 이메일의 @ 앞부분을 쓴다
- 변경 계기 또는 확인된 근거: 이메일 앞부분은 계정 주소에서 파생된 값이라
  사람 이름을 담을 수 없다. 컬럼 하나와 폴백 한 줄이면 되고, 응답 필드는
  name 하나로 그대로여서 프론트 계약은 바뀌지 않는다
- 팀이 직접 밝힌 변경 이유: 화면에 실제 이름이 나오게 하자는 요청
- 영향을 받는 문서·기능: 13.1, backend/app/db/schema.sql, /auth/me
- 아직 결정하지 않은 항목: 이름을 넣는 경로. 회원가입 API 가 없어 지금은
  계정과 함께 DB 에 직접 넣는다
```

컬럼은 nullable 이라 기존 계정에 영향이 없다. 이름을 넣지 않은 계정은 종전과
같은 값을 계속 받는다.
