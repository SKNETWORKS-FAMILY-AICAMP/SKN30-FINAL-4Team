[project]
name = "notice-semantic-structuring"
version = "0.1.0"
description = "Contracts and validators for grant-notice semantic structuring"
requires-python = ">=3.11"
dependencies = [
  "jsonschema>=4.23,<5",
  "openai>=2.26,<3",
  "pydantic>=2.10,<3",
  "python-dotenv>=1.1,<2",
]

[tool.uv]
package = false
