# Isolated Model 1 HTTP service.  Torch is deliberately inherited from the
# CUDA-enabled RunPod image through a --system-site-packages venv; do not let
# pip replace that GPU build with a generic wheel.
fastapi==0.115.14
starlette==0.46.2
uvicorn==0.52.4
# The frozen dl07 preprocessing module imports these at module import time.
# Keep the CPU-parity versions in this venv; neither has CUDA ownership.
numpy==2.4.4
pandas==3.0.3
transformers==5.13.0
safetensors==0.8.0
tokenizers==0.22.2
