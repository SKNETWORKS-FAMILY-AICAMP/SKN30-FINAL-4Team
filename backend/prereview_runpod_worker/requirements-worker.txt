# GPU client / RunPod handler environment.
#
# Install this file only in .venv-surya-client, which is created with
# --system-site-packages so it reuses the verified RunPod A100 Torch runtime.
# Do not install it into the global Python or the vLLM environment.
#
# Surya 0.22.1 is deliberately installed without dependency resolution by the
# setup script: letting a resolver choose its own Torch wheel can replace the
# pod's working CUDA build.  Its non-Torch runtime surface is pinned here.
surya-ocr==0.22.1
Pillow==10.4.0
opencv-python-headless==4.11.0.86

# Explicit non-Torch runtime closure verified on the A100 pod.  The setup
# script installs this file with --no-deps; this keeps Surya's dependency
# metadata from replacing the global CUDA Torch/torchvision pair.
filetype==1.2.0
httpx==0.28.1
jsonschema==4.26.0
numpy==2.5.3
pydantic==2.13.5
pydantic-settings==2.15.0
pypdfium2==5.13.0
python-dotenv==1.2.3
PyYAML==6.0.3
requests==2.34.2
safetensors==0.8.0
tqdm==4.70.1
transformers==5.16.1
regex==2026.9.10
typer==0.27.2

# Direct Surya dependencies, retained explicitly so the client setup can use
# --no-deps safely.  These packages have no Torch/CUDA authority.
annotated-types==0.8.0
anyio==4.15.1
beautifulsoup4==4.14.2
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
distro==1.9.0
filelock==3.32.7
h11==0.16.0
httpcore==1.0.9
huggingface-hub==1.31.0
fsspec==2026.7.0
hf-xet==1.6.0
idna==3.19
jiter==0.17.0
jsonschema-specifications==2025.9.1
openai==2.54.0
packaging==26.3
platformdirs==4.11.8
pydantic-core==2.46.5
referencing==0.37.0
rpds-py==2026.6.3
sniffio==1.3.1
soupsieve==2.8
tokenizers==0.23.2
typing-extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.8.0

# Persistent-Pod API.  Uvicorn is deliberately installed without its
# optional reload/HTTP accelerator extras; the service is internal,
# loopback-only, single-process, and supervised separately.
fastapi==0.115.14
starlette==0.46.2
uvicorn==0.52.4
socksio==1.0.0
